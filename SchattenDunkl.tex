\documentclass{amsart}
\usepackage{amssymb, color}
\usepackage{mathrsfs}
\usepackage{esint}
\usepackage{enumerate}


%\usepackage{txfonts} %also pxfonts
\usepackage[colorlinks, citecolor=blue,pagebackref,hypertexnames=false]{hyperref}




\begin{document}


\newtheorem{cl}{Claim}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{coro}[theorem]{Corollary}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{nota}[theorem]{Notation}
%\theoremstyle{definition}
\newtheorem{assum}{Assumption}[section]
\newtheorem{example}[theorem]{Example}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{construction}[theorem]{Construction}


\renewcommand{\theequation}
{\thesection.\arabic{equation}}

\def\SL{\sqrt H}


\newcommand{\as}[1]{{\mar{AS:#1}}}
\newcommand{\cE}{\mathcal{E}}
\newcommand\R{\mathbb{R}}
\newcommand\RR{\mathbb{R}}
\newcommand\CC{\mathbb{C}}
\newcommand\D {{\rm Dunkl}}
\newcommand\NN{\mathbb{N}}
\newcommand\N {{\bf N}}
\newcommand\ZZ{\mathbb{Z}}
\newcommand\HH{\mathbb{H}}
\newcommand\Z{\mathbb{Z}}
\def\RN {\mathbb{R}^n}
\renewcommand\Re{\operatorname{Re}}
\renewcommand\Im{\operatorname{Im}}

\newcommand{\mc}{\mathcal}

\def\hs{\hspace{0.33cm}}
\newcommand{\la}{\alpha}
\def \l {\alpha}
\newcommand{\eps}{\tau}
\newcommand{\pl}{\partial}
\newcommand{\supp}{{\rm supp}{\hspace{.05cm}}}
\newcommand{\x}{\times}
\newcommand{\lag}{\langle}
\newcommand{\rag}{\rangle}

\newcommand\wrt{\,{\rm d}}

\newcommand{\norm}[2]{|#1|_{#2}}
\newcommand{\Norm}[2]{\|#1\|_{#2}}

\title[Schatten class properties of Dunkl--Riesz transform commutators]{Weyl--Chamber geometry, Poincar\'e inequality meets Schatten class properties of Dunkl--Riesz transform commutators}



\author{Zhijie Fan}
\address{Zhijie Fan, School of Mathematical Sciences, South China Normal University, Guangzhou 510631, China}
\email{fanzhj3@mail2.sysu.edu.cn}







\author{Ji Li}
\address{Ji Li, Department of Mathematics, Macquarie University,NSW2109, Australia}
\email{ji.li@mq.edu.au}



\author{Dmitriy Zanin}
\address{Dmitriy Zanin, University of New South Wales, Kensington, NSW 2052, Australia}
\email{d.zanin@unsw.edu.au}






  \date{\today}

 \subjclass[2010]{47B10, 42B20, 43A85}
\keywords{Schatten class, Riesz transform commutators, Dunkl operator, Besov space, Nearly weakly orthogonal sequence}




\begin{abstract}

\end{abstract}

\maketitle


\tableofcontents

%\newpage
\section{Introduction}
\subsection{Background and motivation}
%In [...], Dunkl introduced so-called Dunkl operators, that is a family of first order differential-diffenence operators which play the role similar to the usual partial diffentiation for the reflection group. The Dunkl theory is an interesting subject in analysis and can be view as a  generalization of Fourier analysis in the Euclidean setting. This foundation can be tracked back by the influential paper of Dunkl  [...]. Since then, significant progress has been made in its development. See for example  [...] and the references therein. For further information, we refer the reader to [] for more information and references. Motivated by the extensive research conducted in the Dunkl settings, in this paper we will develop the harmonic analysis on the Dunkl setting. Firstly we would like to introduce the framework of the Dunkl setting in [...] (see also [...]). \\
{\color{red}[Zhijie will write the background in the Dunkl setting.]}


\begin{theorem}\label{main theorem} Let $R$ be a normalised root system in $\mathbb{R}^N$ and let $G$ be the corresponding finite reflection group. Let $k:R\to\mathbb{R}_+$ be $G$-invariant multiplicity function. Let $R_{{\rm Dunkl},j}$ be the $j$-th Dunkl-Riesz transform corresponding to $R$ and $k.$ Let $w$ be the Dunkl weight corresponding to $R$ and $k.$ Let $f\in L_1^{{\rm loc}}(\mathbb{R}^N).$
\begin{enumerate}[{\rm (i)}]
\item\label{mta} If $f\in\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N),$ $p>N,$ then $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)).$ Furthermore,
$$\|[R_{{\rm Dunkl},j},M_f]\|_{\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx))}\leq c_{p,R,k}\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)};$$
\item\label{mtb} If $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)),$ $p>N,$ then $f\in\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N).$ Furthermore,
$$\|[R_{{\rm Dunkl},j},M_f]\|_{\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx))}\leq c_{p,R,k}^{-1}\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)};$$
\item\label{mtc} If $f\in\dot{W}^{1,N}(\mathbb{R}^N),$ then $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_{N,\infty}(L_2(\mathbb{R}^N,w(x)dx)).$ Furthermore,
$$\|[R_{{\rm Dunkl},j},M_f]\|_{\mathcal{L}_{N,\infty}(L_2(\mathbb{R}^N,w(x)dx))}\leq c_{R,k}\|f\|_{\dot{W}^{1,N}(\mathbb{R}^N)};$$
\item\label{mtd} If $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_{N,\infty}(L_2(\mathbb{R}^N,w(x)dx)),$ then $f\in\dot{W}^{1,N}(\mathbb{R}^N).$ Furthermore,
$$\|[R_{{\rm Dunkl},j},M_f]\|_{\mathcal{L}_{N,\infty}(L_2(\mathbb{R}^N,w(x)dx))}\geq c_{R,k}^{-1}\|f\|_{\dot{W}^{1,N}(\mathbb{R}^N)};$$
\end{enumerate}
\end{theorem}




\subsection{Notation}
Conventionally, we let $\mathbb{N}$ denote the set of positive integers and set $\mathbb{Z}_+ := \{0\} \cup \mathbb{N}$. Throughout the paper, the symbols $C$ and $c$ denote positive constants (possibly varying from line to line) that may depend only on $N$, $p$, and the structural data $(R,k)$ of the Dunkl setting, but are independent of the main parameters (e.g., the symbol $b$ and auxiliary decay factors).
 We write $A \lesssim B$ to mean that $A \leq C B$ for some constant $C > 0$, and $A \sim B$ to mean that both $A \lesssim B$ and $B \lesssim A$ hold.
For \(\lambda>0\) and a cube \(Q\) with sidelength \(\ell(Q)\), write \(\lambda Q\) for the cube concentric with \(Q\) and sidelength \(\lambda\,\ell(Q)\). Similarly, for every ball \(B=B(x,r)\), set \(\lambda B:=B(x,\lambda r)\). For any $1\leq p\leq\infty$, we denote by $p'$ the conjugate of $p$, which satisfies $\frac{1}{p}+\frac{1}{p'}=1$. We denote the average of a function $f$ over a $\mu$-measurable set $E$ by
\begin{align}\label{defaverage}
(f)_E:=\fint_{E}f(x)d\mu(x):=\frac{1}{\mu(E)}\int_{E}f(x)d\mu(x).
\end{align}
For a subset $E \subset \mathbb{R}^{N}$, we denote by $\chi_E$ its characteristic function.

Denote by $\{e_j\}_{j=1}^N$ the standard orthonormal basis on $\mathbb{R}^N$.
%For two measurable Borel sets $E$ and $F$, we use the notation ${\rm dist}(E,\,F)$ to denote the distance between them.


\section{Preliminaries}
\setcounter{equation}{0}
\subsection{Harmonic analysis in the Dunkl setting}
Let $\mathbb{R}^N$ be the Euclidean space equipped with the usual scalar product $\langle \cdot,\,\cdot\rangle $ and the induced norm $\|\cdot\|$. For a nonzero vector $v\in \mathbb{R}^N$, the reflection $\sigma_v$ with respect to the hyperplane orthogonal to a normalised vector $v$ is defined by
\begin{align*}
	\sigma_v x:= x-2\langle x,v\rangle v.
\end{align*}
A finite set $R\subset \mathbb{R}^N\backslash \{0\}$ is called a root system if $\sigma_v(R)=R$ for every $v\in R$. A root system $R$ is called a normalized reduced root system if  $|v|=1$ for every $v\in R.$
The finite group $G$ generated by the reflections $\{\sigma_v\}_{v\in R}$ is called the Weyl group (reflection group) of the root system $R$.
Let $k: R \to [0,\infty)$ be a multiplicity function, which is a $G$-invariant function  fixed throughout the paper.

Define the Dunkl measure $w(x)dx$ on $\mathbb{R}^N$ by
$$w(x)=\prod_{v \in R} |\langle x,v\rangle|^{k(v)}.$$
Let $B(x,r):=\{y\in \mathbb{R}^N:\|x-y\|< r\}$ be the Euclidean ball centred at $x$ with radius $r>0$. Let $\N:=N+\sum_{v\in R}k(v)$. Note that
$$w(B(tx,tr))=t^{\N}w(B(x,r)),\ {\rm for}\ x\in\mathbb{R}^N,\ t,r>0.$$
Furthermore,
\begin{align}\label{twosideinequ}
  w(B(x,r))\sim r^N \prod_{v \in R}(|\langle x, v \rangle |+r)^{k(v)},
\end{align}
where the implicit constant only depends on $N,R$ and $k$.  It follows from \eqref{twosideinequ} that there exists a constant $C\geq 1$ such that for every $x\in \mathbb{R}^N$,
\begin{align}\label{doublinginequality}
C^{-1}\left(\frac{r_2}{r_1}\right)^N\leq \frac{w(B(x,r_2))}{w(B(x,r_1))}\leq C\left(\frac{r_2}{r_1}\right)^\N,\ {\rm for}\ 0<r_1<r_2,
\end{align}
so the numbers $\N$ and $N$ are called the upper and lower homogeneous dimensions, respectively.

The Dunkl operators $T_\xi$ are defined by
$$T_\xi f(x):=\partial_\xi f(x)+\sum_{v\in R}\frac{k(v)}{2}\langle v,\xi\rangle\frac{f(x)-f(\sigma_v(x))}{\langle v,x\rangle},$$
where $\partial_\xi f$ denotes the directional derivative of $f$ in the direction $\xi$. Furthermore, the Dunkl Laplacian is defined by $$\Delta_{\D}f(x):=\sum_{j=1}^N T_{e_j}^2f(x)=\Delta_{\mathbb{R}^N}f(x)+\sum_{v\in R}k(v)\delta_v f(x),$$
where $\Delta_{\mathbb{R}^N}$ is the Laplacian on $\mathbb{R}^N$ and $\delta_v$ is defined by
$$\delta_v f(x):=\frac{\partial_v f(x)}{\langle v,x\rangle}-\frac{f(x)-f(\sigma_v(x))}{\langle v,x\rangle^2}.$$
In this article, we consider the Dunkl-Riesz transforms defined by $R_{\D,j}:=T_{e_j}(-\Delta_{\D})^{-1/2}$, $j=1,2,\ldots,N$.


\begin{definition}{\rm
The Weyl chambers are defined as the closures of connected components of the set
  \begin{align}
    \left\{x\in\mathbb{R}^N:\langle x,v\rangle \neq 0\ \ { for} \ { all} \ v\in R\right\}.\end{align}}
\end{definition}
Define
$$d(x,y)=\min_{\sigma\in G}|x-\sigma(y)|,\quad x,y\in\mathbb{R}^N.$$
Recall from \cite[Chapter VII, proof of Theorem 2.12]{MR514561} that
\begin{align}\label{distanceequa}
d(x,y)=\|x-y\|\ {\rm if}\ {\rm and}\ {\rm only}\ {\rm if}\ x,y\in\Gamma
\end{align}
for some closed Weyl chamber $\Gamma$.


Denote by $K_{\D,j}(x,y)$ the integral kernel of $R_{\D,j}$. We recall the kernel estimates for the Riesz transforms in the Dunkl setting proved in \cite{MR4733961}. Although these estimates may not be sharp (see the comment following Theorem~2.1 in \cite[p.~10]{MR4733961}), they suffice for our purposes.
\begin{lemma}\label{kernelestimate}
There exists a constant $C$ such that for $j\in\{1,2,\cdots,N\}$ and every $x,y$ with $d(x,y)\neq 0,$
\begin{align*}
|K_{\D,j}(x,y)|\leq \frac{d(x,y)}{|x-y|}\cdot\frac{C}{w(B(x,d(x,y)))}.
\end{align*}
\end{lemma}
\begin{proof}
The proof is given in \cite[Theorem 1.1]{MR4733961}.
\end{proof}

\begin{lemma}\label{for sufficiency auxiliary lemma} We have
$$d(x,y)^2\cdot w(x)w(y)\leq Cw^2(B(x,d(x,y))),\quad x,y\in\mathbb{R}^N.$$
\end{lemma}
\begin{proof} Define equivalence relation $\sim$ on $R$ by saying $v_1\sim v_2$ if there exists $g\in G$ with $v_1=gv_2.$ Let us split $R$ into equivalence classes with respect to $\sim.$ Since $k$ is $G$-invariant, it follows that $k$ is constant on each equivalence class. Note that $G$ leaves each equivalence class invariant.

We, therefore, write
$$w(x)=\prod_{A\in R/\sim}\big(\prod_{v\in A}|\langle x,v\rangle|\big)^{k|_A}.$$

On the other hand,
$$w(B(x,r))\geq c_{R,k}^{(1)}r^N\prod_{A\in R/\sim}\big(\prod_{v\in A}(r+|\langle x,v\rangle|)\big)^{k|_A}.$$

Let $x\in\Gamma$ and $y\in g\Gamma.$ Set $z=g^{-1}y\in\Gamma.$ Note that $d(x,y)=|x-z|$ and, therefore,
$$|\langle x,v\rangle|+d(x,y)=|\langle x,v\rangle|+|x-z|\geq 2^{-\frac12}|\langle z,v\rangle|.$$
Thus,
$$\prod_{v\in A}(|\langle x,v\rangle|+d(x,y))\geq\prod_{v\in A}2^{-\frac12}|\langle z,v\rangle|=2^{-\frac12|A|}\prod_{v\in A}|\langle g^{-1}y,v\rangle|=$$
$$=2^{-\frac12|A|}\prod_{v\in A}|\langle y,gv\rangle|=2^{-\frac12|A|}\prod_{v\in gA}|\langle y,v\rangle|=2^{-\frac12|A|}\prod_{v\in A}|\langle y,v\rangle|.$$
Thus,
$$w(B(x,d(x,y)))\geq c_{R,k}^{(1)}d^N(x,y)\prod_{A\in R/\sim}\big(\prod_{v\in A}(d(x,y)+|\langle x,v\rangle|)\big)^{k|_A}\geq$$
$$\geq c_{R,k}^{(1)}d^N(x,y)\prod_{A\in R/\sim}\big(2^{-\frac12|A|}\prod_{v\in A}|\langle y,v\rangle|\big)^{k|_A}=c_{R,k}^{(2)}d^N(x,y)w(y).$$
On the other hand,
$$w(B(x,d(x,y)))\geq c_{R,k}^{(1)}d^N(x,y)w(x).$$
Multiplying the last two equations, we complete the proof.
\end{proof}

\begin{lemma}\label{nondegenerate} Let $1\leq j\leq N.$ If $x_j\geq y_j,$ then
$$K_{\D,j}(x,y)\geq c_{R,k}\frac{x_j-y_j}{|x-y|}\cdot\frac1{w(B(x,|x-y|))}.$$
If $x_j\leq y_j,$ then
$$K_{\D,j}(x,y)\leq c_{R,k}\frac{x_j-y_j}{|x-y|}\cdot\frac1{w(B(x,|x-y|))}.$$
\end{lemma}
\begin{proof} The lower bound is identical to that in \cite[Theorem 1.2]{MR4733961}.
\end{proof}

\subsection{Schatten class}
{\color{red}[Zhijie will add this later]}





\subsection{Nearly weakly  orthonormal sequences}
Rochberg and Semmes \cite{RS} introduced the notion of \emph{nearly weakly  orthonormal (NWO)} sequences of functions. This notion has since proved highly effective in characterizing Schatten class membership of  singular integral commutators in a variety of contexts (see \cite{FLLXarxiv,HKarxiv,MR4756021,MR4552581,MR4873796,LLW,RS,Zhangwei1,Zhangwei2}). The properties of NWO sequences were originally investigated in the Euclidean setting (equipped with Lebesgue measure), but the extension to spaces of homogeneous type is straightforward once a system of dyadic cubes is available in this general framework (see \cite[Section 13]{HKarxiv} for a complete proof).  For our purposes, we adapt  the extension to the Dunkl setting $(\mathbb{R}^N,\|\cdot\|,w)$ rather than recalling the general  definition
of NWO sequences and the general extension to spaces of homogeneous type.

{\it Let $\mathscr{D}$ be a system of dyadic cubes. A sequence of functions $\{e_Q\}_{Q\in\mathscr{D}}$ is an NWO sequence if
\begin{enumerate}
  \item $\supp e_Q\subseteq cQ$ for some positive constant $c$ independent of $Q$
  \item $\|e_Q\|_{L^s(\mathbb{R}^{N},dw)}\leq w(Q)^{1/s-1/2}$ for some $s>2$.
\end{enumerate}
}
\begin{lemma}\label{NWOLEMMAA}
Let $1<p<\infty$, and let $\mathscr{D}$ be a
system of dyadic cubes over $\mathbb{R}^N$. For any compact operator $ T $ on $ L ^2 (\mathbb R ^{N},dw)$, we have
\begin{equation*}
\Bigl(
\sum_{Q\in \mathscr{D}} |\langle T e_Q, f_Q \rangle_{L^2(\mathbb{R}^N,dw)} | ^{p}
\Bigr) ^{1/p} \leq C_{N,p,w} \| T \| _{S ^{p}},
\end{equation*}
where $\{e_Q\}_{Q\in\mathscr{D}}$ and $\{f_Q\}_{Q\in\mathscr{D}}$ are NWO sequences.
\end{lemma}
\begin{proof}
This property can be found in  \cite[(1.10)]{RS} (see also \cite[Section 7]{HKarxiv}).
\end{proof}

\begin{lemma}\label{NWOLEMMAA2}
Let $0<p<\infty$, and let $\mathscr{D}$ be a
system of dyadic cubes over $\mathbb{R}^N$. Assume that
$$A=\sum_{Q\in\mathscr{D}}\lambda_Q\langle \cdot,\,e_Q\rangle_{L^2(\mathbb{R}^N,dw)} f_Q,$$
where $\{e_Q\}_{Q\in\mathscr{D}}$ and $\{f_Q\}_{Q\in\mathscr{D}}$ are NWO sequences and $\{\lambda_Q\}_{Q\in\mathscr{D}}$ is a sequence of scalars. We have
\begin{align}\label{nwose}
\|A\|_{S^{p}}\leq C_{N,p,w} \|\{\lambda_Q\}_Q\|_{\ell^{p}}.
\end{align}
\end{lemma}
\begin{proof}
This property can be found in  \cite[(1.9)]{RS} (see also \cite[Section 7]{HKarxiv}).
\end{proof}

\section{Comparison of Sobolev norms}

\begin{theorem}\label{sobolev comparison theorem} For every $f\in\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N),$ $p>N,$ we have
$$\left(\int_{\mathbb{R}^N\times\mathbb{R}^N}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\right)^{\frac1p}\leq c_{p,R}\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}.$$
\end{theorem}

\begin{lemma}\label{s4 first compute lemma} For every convex polyhedral cone $\Gamma,$ we have
$$\int_{\Gamma}\frac{dx}{|x-y|^{2N}}\geq c_{\Gamma}{\rm dist}(y,\Gamma)^{-N},\quad y\notin\Gamma.$$
\end{lemma}
\begin{proof} Find a point $z\in\Gamma$ such that
$$|y-z|={\rm dist}(y,\Gamma).$$
We have $z+\Gamma\subset\Gamma.$ Thus,
$$\int_{\Gamma}\frac{dx}{|x-y|^{2N}}\geq \int_{z+\Gamma}\frac{dx}{|x-y|^{2N}}=\int_{\Gamma}\frac{dx}{|x-(y-z)|^{2N}}.$$
Clearly,
$$|x-(y-z)|\leq |x|+|y-z|.$$
Thus,
$$\int_{\Gamma}\frac{dx}{|x-y|^{2N}}\geq\int_{\Gamma}\frac{dx}{(|x|+|y-z|)^{2N}}=|y-z|^{-N}\int_{\Gamma}\frac{dx}{(|x|+1)^{2N}}.$$
\end{proof}

\begin{lemma}\label{s4 second compute lemma} If $p\in(N,2N),$ then
$$\sup_{a>0}\int_{\mathbb{R}^N}\frac{|x-ae_N|^{p-2N}dx}{(1+|x|)^p}<\infty.$$
\end{lemma}
\begin{proof} {\bf Step 1:} we claim that, for every $a>0,$
$$\int_{\mathbb{R}^N}\frac{|x-ae_N|^{p-2N}dx}{(1+|x|)^p}\leq$$
$$\leq 4^p\int_{\mathbb{R}^{N-1}\times(-a,a)}\frac{|x|^{p-2N}dx}{(1+|x'|+a)^p}+3^{2N-p}\int_{\mathbb{R}^N}\frac{|x|^{p-2N}dx}{(1+|x|)^p}.$$


We clearly have
$$\int_{\mathbb{R}^N}\frac{|x-ae_N|^{p-2N}dx}{(1+|x|)^p}=$$
$$=\int_{\mathbb{R}^{N-1}\times(\frac{a}{2},\frac{3a}{2})}\frac{|x-ae_N|^{p-2N}dx}{(1+|x|)^p}+\int_{\mathbb{R}^{N-1}\times\mathbb{R}\backslash(\frac{a}{2},\frac{3a}{2})}\frac{|x-ae_N|^{p-2N}dx}{(1+|x|)^p}.$$

If $x_N\notin(\frac{a}{2},\frac{3a}{2}),$ then $|x_N-a|\geq \frac13|x_N|.$ Hence, $|x-ae_N|\geq \frac13|x|.$ Thus,
$$\int_{\mathbb{R}^{N-1}\times\mathbb{R}\backslash(\frac{a}{2},\frac{3a}{2})}\frac{|x-ae_N|^{p-2N}dx}{(1+|x|)^p}\leq$$
$$\leq \int_{\mathbb{R}^{N-1}\times\mathbb{R}\backslash(\frac{a}{2},\frac{3a}{2})}\frac{3^{2N-p}|x|^{p-2N}dx}{(1+|x|)^p}\leq \int_{\mathbb{R}^N}\frac{3^{2N-p}|x|^{p-2N}dx}{(1+|x|)^p}.$$

If $x_N\in(\frac{a}{2},\frac{3a}{2}),$ then (here, we using the notation $x'=(x_1,\cdots,x_{N-1})$) $$1+|x|\geq1+\max\{|x'|,\frac12a\}\geq\frac14(1+|x'|+a).$$
Thus,
$$\int_{\mathbb{R}^{N-1}\times(\frac{a}{2},\frac{3a}{2})}\frac{|x-ae_N|^{p-2N}dx}{(1+|x|)^p}\leq 4^p\int_{\mathbb{R}^{N-1}\times(\frac{a}{2},\frac{3a}{2})}\frac{|x-ae_N|^{p-2N}dx}{(1+|x'|+a)^p}=$$
$$=4^p\int_{\mathbb{R}^{N-1}\times(-\frac{a}{2},\frac{a}{2})}\frac{|x|^{p-2N}dx}{(1+|x'|+a)^p}\leq 4^p\int_{\mathbb{R}^{N-1}\times(-a,a)}\frac{|x|^{p-2N}dx}{(1+|x'|+a)^p}.$$

Combining all preceding estimates, we infer the claim in Step 1.

{\bf Step 2:} we claim that
$$\sup_{a>0}\int_{\mathbb{R}^{N-1}\times(-a,a)}\frac{|x|^{p-2N}dx}{(1+|x'|+a)^p}<\infty.$$

Passing to polar coordinates in $\mathbb{R}^{N-1},$ we equivalently rewrite the claim as
$$\sup_{a>0}\int_{\mathbb{R}_+\times(0,a)}\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}drdx_N}{(1+r+a)^p}<\infty.$$

If $0<a<1,$ then
$$\int_{\mathbb{R}_+\times(0,a)}\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}drdx_N}{(1+r+a)^p}\leq \int_{\mathbb{R}_+\times(0,1)}\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}drdx_N}{(1+r)^p}.$$

If $a\geq 1,$ then
$$\int_{\mathbb{R}_+\times(0,a)}\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}drdx_N}{(1+r+a)^p}\leq \int_{\mathbb{R}_+\times(0,a)}\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}drdx_N}{(r+a)^p}=$$
$$=\int_{(a,\infty)\times(0,a)}\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}drdx_N}{(r+a)^p}+$$
$$+\int_0^a\int_0^r\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}dx_Ndr}{(r+a)^p}+\int_0^a\int_0^{x_N}\frac{(r^2+x_N^2)^{\frac{p}{2}-N}r^{N-2}drdx_N}{(r+a)^p}\leq$$
$$\leq\footnote{It is exactly at this point we are using the assumption $p<2N.$}\int_{(a,\infty)\times(0,a)}\frac{r^{p-2N}r^{N-2}drdx_N}{r^p}+$$
$$+\int_0^a\int_0^r\frac{r^{p-2N}r^{N-2}dx_Ndr}{a^p}+\int_0^a\int_0^{x_N}\frac{x_N^{p-2N}r^{N-2}drdx_N}{a^p}=$$
$$=\int_a^{\infty}r^{-N-1}dr+a^{-p}\int_0^ar^{p-N-1}dr+\frac1{N-1}a^{-p}\int_0^ax_N^{p-N-1}dx_N=$$
$$=\frac1Na^{-N}+\frac1{p-N}a^{-N}+\frac1{(N-1)(p-N)}a^{-N}\leq \frac1N+\frac1{p-N}+\frac1{(N-1)(p-N)}.$$
This yields the claim in Step 2.

Combining Steps 1 and 2, we complete the proof of the lemma.
\end{proof}


\begin{lemma}\label{s4 third compute lemma} If $p\in(N,2N),$ then $$\int_{\mathbb{R}^{N-1}\times\mathbb{R}_+}\frac{|x-y|^{p-2N}dx}{|x+z|^p}\leq c_{p,N}z_N^{-N},\quad y,z\in\mathbb{R}^{N-1}\times\mathbb{R}_+.$$
\end{lemma}
\begin{proof} By dilation invariance, we may assume without loss of generality that $z_N=1.$ By translation invariance in $\mathbb{R}^{N-1},$ we may assume without loss of generality that $z_1=\cdots=z_{N-1}=0.$ We, therefore, have
$$|x+z|=|x+e_N|\geq \max\{1,|x|\}\geq\frac12(1+|x|),\quad x\in\mathbb{R}^{N-1}\times\mathbb{R}_+.$$
Thus,
$$\int_{\mathbb{R}^{N-1}\times\mathbb{R}_+}\frac{|x-y|^{p-2N}dx}{|x+z|^p}\leq 2^p\int_{\mathbb{R}^{N-1}\times\mathbb{R}_+}\frac{|x-y|^{p-2N}dx}{(1+|x|)^p}\leq$$
$$=2^p\int_{\mathbb{R}^N}\frac{|x-y|^{p-2N}dx}{(1+|x|)^p}=2^p\int_{\mathbb{R}^N}\frac{\big|x-|y|e_N\big|^{p-2N}dx}{(1+|x|)^p}.$$
The assertion follows from Lemma \ref{s4 second compute lemma}.
\end{proof}


We need the following elementary version of the Hahn-Banach theorem.

\begin{lemma}\label{hahn-banach lemma} Let $K\subset\mathbb{R}^N$ be a convex set and let $y\notin K.$ Let $z\in K$ be the point closest to $y.$ Affine hyperplane $P$ passing through $z$ and orthogonal to $y-z$ splits the space into two half-spaces, one of them containing $y,$ the other one containing $K.$ 
\end{lemma}
\begin{proof} By translation invariance, we may assume without loss of generality that $z=0.$ By rotation and dilation invariance, we may assume without loss of generality that $y=e_N.$ By assumption, ${\rm dist}(e_N,K)=1.$ We need to show that $K$ is contained in the (closed) lower half-plane.

Assume the contrary: let $x\in K$ be such that $x_N>0.$ Since $0\in K$ and since $K$ is convex, it follows that $\epsilon x\in K$ for every $\epsilon\in(0,1).$ Thus,
$${\rm dist}(e_N,K)\leq |e_N-\epsilon x|,\quad \epsilon\in(0,1).$$
Since $\epsilon\in(0,1)$ is arbitrary, it follows that
$${\rm dist}(e_N,K)\leq \inf_{\epsilon\in(0,1)}|e_N-\epsilon x|=\inf\big(\epsilon^2\sum_{k=1}^{N-1}x_k^2+(1-\epsilon x_N)^2\big)^{\frac12}.$$
Since $x_N>0,$ it follows that infimum is strictly less than $1.$ Thus, ${\rm dist}(e_N,K)<1.$ However, it is granted in the lemma that ${\rm dist}(e_N,K)=1.$ This contradiction shows that such an $x$ cannot exist. This completes the proof.
\end{proof}


\begin{lemma}\label{s4 main lemma} If $p\in(N,2N),$ then
$$\int_{\Gamma}\frac{|x-y|^{p-2N}dx}{|x-gy|^p}\leq c_p{\rm dist}(gy,\Gamma)^{-N},\quad y\in\Gamma.$$
\end{lemma}
\begin{proof} Find a point $z\in\Gamma$ such that
$$|gy-z|={\rm dist}(gy,\Gamma).$$
By Lemma \ref{hahn-banach lemma}, affine hyperplane passing through $z$ and orthogonal to $gy-z$ splits the space into two half-spaces, one of them containing $gy,$ the other one containing $\Gamma$ (denote this one by $H$).

We have $y\in H,$ $\Gamma\subset H$ and $gy\notin H.$ So,
$$\int_{\Gamma}\frac{|x-y|^{p-2N}dx}{|x-gy|^p}\leq\int_H\frac{|x-y|^{p-2N}dx}{|x-gy|^p}.$$
Using Lemma \ref{s4 third compute lemma}, we write
$$\int_H\frac{|x-y|^{p-2N}dx}{|x-gy|^p}\leq c_{p,N}{\rm dist}(gy,H)^{-N}.$$
Since $z\in \partial H$ and since $gy-z$ is orthogonal to $\partial H,$ it follows that ${\rm dist}(gy,H)=|gy-z|={\rm dist}(gy,\Gamma).$
\end{proof}


\begin{proof}[Proof of Theorem \ref{sobolev comparison theorem}] {\bf Case 1:} Let $p\in(N,2N).$

We have
$$\Big(\int_{\Gamma\times g\Gamma}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\Big)^{\frac1p}=$$
$$=\Big(\int_{\Gamma\times \Gamma}\Big(\frac{|x-y|}{|x-gy|}\Big)^p|f(x)-f(gy)|^p \frac{dxdy}{|x-y|^{2N}}\Big)^{\frac1p}\leq$$
$$\leq\Big(\int_{\Gamma\times \Gamma}\Big(\frac{|x-y|}{|x-gy|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{|x-y|^{2N}}\Big)^{\frac1p}+$$
$$+\Big(\int_{\Gamma\times \Gamma}\Big(\frac{|x-y|}{|x-gy|}\Big)^p|f(y)-f(gy)|^p \frac{dxdy}{|x-y|^{2N}}\Big)^{\frac1p}\leq$$
$$\leq\Big(\int_{\Gamma\times \Gamma}|f(x)-f(y)|^p \frac{dxdy}{|x-y|^{2N}}\Big)^{\frac1p}+$$
$$+\Big(\int_{\Gamma}|f(y)-f(gy)|^p\Big(\int_{\Gamma}\frac{|x-y|^{p-2N}}{|x-gy|^p}dx\Big)dy\Big)^{\frac1p}.$$
Obviously, the first summand on the right hand side is less than $\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}.$ By Lemmas \ref{s4 main lemma} and \ref{s4 first compute lemma},
$$\Big(\int_{\Gamma\times g\Gamma}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\Big)^{\frac1p}\leq \|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}+$$
$$+c_{p,R}^{(1)}\Big(\int_{\Gamma}|f(y)-f(gy)|^p\Big(\int_{\Gamma}\frac{dx}{|x-gy|^{2N}}\Big)dy\Big)^{\frac1p}=$$
$$=\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}+c_{p,R}^{\frac1p}\Big(\int_{\Gamma\times \Gamma}\frac{|f(y)-f(gy)|^pdxdy}{|x-gy|^{2N}}\Big)^{\frac1p}\leq$$
$$\leq\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}+c_{p,R}^{(1)}\Big(\int_{\Gamma\times \Gamma}\frac{|f(x)-f(gy)|^pdxdy}{|x-gy|^{2N}}\Big)^{\frac1p}+$$
$$+c_{p,R}^{(1)}\Big(\int_{\Gamma\times \Gamma}\frac{|f(x)-f(y)|^pdxdy}{|x-gy|^{2N}}\Big)^{\frac1p}.$$
Now,
$$\Big(\int_{\Gamma\times \Gamma}\frac{|f(x)-f(gy)|^pdxdy}{|x-gy|^{2N}}\Big)^{\frac1p}=\Big(\int_{\Gamma\times g\Gamma}\frac{|f(x)-f(y)|^pdxdy}{|x-y|^{2N}}\Big)^{\frac1p}\leq \|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}$$
and
$$\Big(\int_{\Gamma\times \Gamma}\frac{|f(x)-f(y)|^pdxdy}{|x-gy|^{2N}}\Big)^{\frac1p}\leq \Big(\int_{\Gamma\times \Gamma}\frac{|f(x)-f(y)|^pdxdy}{|x-y|^{2N}}\Big)^{\frac1p}\leq \|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}.$$
Thus,
$$\Big(\int_{\Gamma\times g\Gamma}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\Big)^{\frac1p}\leq (1+2c_{p,R}^{(1)}) \|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}.$$
Summing over $\Gamma$ and $1\neq g\in G,$ we obtain
$$\left(\int_{\mathbb{R}^N\times\mathbb{R}^N}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\right)^{\frac1p}\leq |G|^2(1+2c_{p,R}^{(1)})\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}.$$

{\bf Case 2:} Let $p\geq 2N.$ We have
$$\Big(\int_{\Gamma\times g\Gamma}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\Big)^{\frac1p}\leq$$
$$\leq\Big(\int_{\Gamma\times g\Gamma}\Big(\frac{d(x,y)}{|x-y|}\Big)^{2N}|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\Big)^{\frac1p}=\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}.$$
\end{proof}



\section{Proof of Theorem \ref{main theorem} \eqref{mta}}
\setcounter{equation}{0}

Let $(X,\mu)$ be a measure space and let $L_p(L_{q,\infty})^{\rm symm}(X\times X,\mu\times\mu)$ denote the mixed-norm space consisting of all $\mu\times\mu$-measurable functions $f:X\times X \to \mathbb{C}$ such that
$$\int_X\|f(x,\cdot)\|_{L_{q,\infty}(X,\mu)}^pd\mu(x),\int_X\|f(\cdot,y)\|_{L_{q,\infty}(X,\mu)}^pd\mu(y)<\infty.$$
We equip $L_p(L_{q,\infty})^{\rm symm}(X\times X,\mu\times\mu)$ with its natural norm.

\begin{lemma}\label{Russo}
Let $(X,\mu)$ be a measure space and let $2<p<\infty.$ Suppose that $K\in L_p(L_{p',\infty})^{\rm symm}(X\times X,\mu\times\mu).$ The integral operator
$$T_Kf(x):=\int_X K(x,y)f(y)d\mu(y)$$
belongs to $S_{p,\infty}(L_2(X,d\mu)).$ Moreover, we have
$$\|T_K\|_{S_{p,\infty}(L_2(X,d\mu))}\leq c_p \|K\|_{L_p(L_{p',\infty})^{\rm symm}(X\times X,\mu\times\mu)}.$$
\end{lemma}
\begin{proof}
The proof is given in \cite[Lemma 2]{JW} (see also \cite[Corollary 11.2]{HKarxiv}).
\end{proof}

\begin{lemma}\label{s4 holder lemma} Let $p>2$ and let $\frac1q=1-\frac2p.$ We have $$\|F_1F_2\|_{L_p(L_{p',\infty})(X\times X,\mu\times\mu)}\leq 2^{\frac1{pp'}}\|F_1\|_{L_p(X\times X,\mu\times\mu)}\|F_2\|_{L_{\infty}(L_{q,\infty})(X\times X,\mu\times\mu)}.$$
\end{lemma}
\begin{proof} We clearly have
$$LHS=\Big(\int_X\|F_1(x,\cdot)F_2(x,\cdot)\|_{L_{p',\infty}(X,\mu)}^pd\mu(x)\Big)^{\frac1p}.$$
Clearly, $\frac1{p'}=\frac1p+\frac1q.$ Thus,
$$\|F_1(x,\cdot)F_2(x,\cdot)\|_{L_{p',\infty}(X,\mu)}
\leq 2^{\frac1{p'}}\|F_1(x,\cdot)\|_{L_{p,\infty}(X,\mu)}\|F_2(x,\cdot)\|_{L_{q,\infty}(X,\mu)}\leq$$
$$\leq 2^{\frac1{p'}}\|F_1(x,\cdot)\|_{L_p(X,\mu)}\|F_2(x,\cdot)\|_{L_{q,\infty}(X,\mu)}.$$
Hence,
$$LHS\leq 2^{\frac1{pp'}}\Big(\int_X\|F_1(x,\cdot)\|_{L_p(X,\mu)}^p\|F_2(x,\cdot)\|_{L_{q,\infty}(X,\mu)}^pd\mu(x)\Big)^{\frac1p}\leq$$
$$\leq 2^{\frac1{pp'}}\Big(\int_X\|F_1(x,\cdot)\|_{L_p(X,\mu)}^pd\mu(x)\Big)^{\frac1p}\times\sup_{x\in X}\|F_2(x,\cdot)\|_{L_{q,\infty}(X,\mu)}=$$
$$=2^{\frac1{pp'}}\|F_1\|_{L_p(X\times X,\mu\times\mu)}\|F_2\|_{L_{\infty}(L_{q,\infty})(X\times X,\mu\times\mu)}.$$
\end{proof}

\begin{lemma}\label{s4 first lemma}
Let $(X,\mu)$ be a measure space and let $2<p<\infty.$ Suppose that $F\in L_p(X\times X,H^2(\mu\times\mu))$ and $H\in L_{\infty}(L_{1,\infty})^{\rm symm}(X\times X,\mu\times\mu).$ We have
$$\|T_{FH}\|_{S_{p,\infty}(L_2(X,d\mu))}\leq c_p \|F\|_{L_p(X\times X,H^2(\mu\times\mu))}\|H\|_{L_{\infty}(L_{1,\infty})^{\rm symm}(X\times X,\mu\times\mu)}^{1-\frac2p}.$$
\end{lemma}
\begin{proof} By Lemma \ref{Russo},
$$\|T_{FH}\|_{S_{p,\infty}(L_2(X,d\mu))}\leq c_p \|FH\|_{L_p(L_{p',\infty})^{\rm symm}(X\times X,\mu\times\mu)}.$$

Let $\frac1q=1-\frac2p.$ It follows from Lemma \ref{s4 holder lemma} that
$$\|F_1F_2\|_{L_p(L_{p',\infty})^{{\rm symm}}(X\times X,\mu\times\mu)}\leq$$
$$\leq 2^{\frac1{pp'}}\|F_1\|_{L_p(X\times X,\mu\times\mu)}\|F_2\|_{L_{\infty}(L_{q,\infty})^{{\rm symm}}(X\times X,\mu\times\mu)}.$$
Setting $F_1=FH^{\frac2p}$ and $F_2=H^{\frac1q},$ we obtain
$$\|FH\|_{L_p(L_{p',\infty})^{\rm symm}(X\times X,\mu\times\mu)}\leq$$
$$\leq 2^{\frac1{pp'}}\|FH^{\frac2p}\|_{L_p(X\times X,\mu\times\mu)}\|H^{\frac1q}\|_{L_{\infty}(L_{q,\infty})^{{\rm symm}}(X\times X,\mu\times\mu)}=$$
$$=2^{\frac1{pp'}}\|F\|_{L_p(X\times X,H^2(\mu\times\mu))}\|H\|_{L_{\infty}(L_{1,\infty})^{{\rm symm}}(X\times X,\mu\times\mu)}^{\frac1q}=$$
$$=2^{\frac1{pp'}}\|F\|_{L_p(X\times X,H^2(\mu\times\mu))}\|H\|_{L_{\infty}(L_{1,\infty})^{{\rm symm}}(X\times X,\mu\times\mu)}^{1-\frac2p}.$$
\end{proof}

\begin{lemma}\label{s4 second lemma}
Let $(X,\mu)$ be a measure space and let $2<p<\infty.$ Suppose that $F\in L_p(X\times X,GH^2(\mu\times\mu))$ and $H\in L_{\infty}(L_{1,\infty})^{\rm symm}(X\times X,\mu\times\mu).$ We have
$$\|T_{FH}\|_{S_p(L_2(X,d\mu))}\leq c_{p,H}\|F\|_{L_p(X\times X,H^2(\mu\times\mu))}.$$
\end{lemma}
\begin{proof} The assertion follows from Lemma \ref{s4 first lemma} by real interpolation.
\end{proof}

The following proposition, strictly speaking, is not part of the proof of Theorem \ref{main theorem} \eqref{mta}. Instead, it features in the proof of Theorem \ref{main theorem} \eqref{mtb} in the next section. However, in terms of the employed technique, it certainly belongs to this section. 

\begin{proposition}\label{upper bound}
Let $p>2,$ let $\Gamma$ be a chamber and let $f\in \dot{W}^{\frac{N}{p},p}(\Gamma).$ Let $V$ be an integral operator whose integral kernel $K$ satisfies the condition
$$|K(x,y)|\leq\frac1{w(B(x,|x-y|))}.$$
For every $1\leq j\leq N,$ we have
$$\|[V,M_f]\|_{\mathcal{L}_p(L_2(\Gamma,w(x)dx))}\leq c_{R,k}\|f\|_{\dot{W}^{\frac{N}{p},p}(\Gamma)}.$$
\end{proposition}
\begin{proof} In what follows, let $X=\Gamma$ and $d\mu(x)=w(x)dx.$ Set
$$F(x,y)=w(B(x,|x-y|))K(x,y)(f(x)-f(y)),\quad x\neq y,$$
$$H(x,y)=\frac1{w(B(x,|x-y|))},\quad x\neq y.$$
By \cite[Lemma 5.7]{HKarxiv}, $H\in L_{\infty}(L_{1,\infty})^{\rm symm}(\Gamma\times\Gamma,\mu\times\mu).$ By assumption on the kernel $K,$
$$\|F\|_{L_p(\Gamma\times\Gamma,G^2(\mu\times\mu))}\leq\Big(\int_{\Gamma\times\Gamma}|f(x)-f(y)|^p\frac{w(x)w(y)dxdy}{w^2(B(x,|x-y|))}\Big)^{\frac1p}.$$
By \eqref{twosideinequ},
$$w(B(x,|x-y|))\geq c_{R,k}'|x-y|^Nw^{\frac12}(x)w^{\frac12}(y),\quad x,y\in\mathbb{R}^N.$$
Thus,
$$\|F\|_{L_p(\Gamma\times\Gamma,H^2(\mu\times\mu))}\leq$$
$$\leq (c_{R,k}')^{-\frac2p}\Big(\int_{\Gamma\times\Gamma}\frac{|f(x)-f(y)|^p}{|x-y|^{2N}}dxdy\Big)^{\frac1p}=(c_{R,k}')^{-\frac2p}\|f\|_{\dot{W}^{\frac{N}{p},p}(\Gamma)}.$$
Obviously, the integral kernel of the operator $[V,M_f]$ is $FH.$ The assertion follows now from Lemma \ref{s4 second lemma}.
\end{proof}

\begin{proof}[Proof of Theorem \ref{main theorem} \eqref{mta}]
In what follows, let $X=\mathbb{R}^N$ and $d\mu(x)=w(x)dx.$ Set
$$F(x,y)=w(B(x,d(x,y)))K_{{\rm Dunkl},j}(x,y)(f(x)-f(y)),\quad x\neq y,$$
$$H(x,y)=\frac1{w(B(x,d(x,y)))},\quad x\neq y.$$
By \cite[Lemma 11.7]{HKarxiv}, $H\in L_{\infty}(L_{1,\infty})^{\rm symm}(X\times X,\mu\times\mu).$ By Lemma \ref{kernelestimate},
$$\|F\|_{L_p(X\times X,H^2(\mu\times\mu))}\leq$$
$$\leq c_{R,k}^{(1)}\left(\int_{\mathbb{R}^N\times\mathbb{R}^N}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p\frac{w(x)w(y)dxdy}{w(B(x,d(x,y)))^2}\right)^{\frac1p}.$$
Using Lemma \ref{for sufficiency auxiliary lemma}, we conclude that
$$\|F\|_{L_p(X\times X,H^2(\mu\times\mu))}\leq c_{R,k}^{(2)}\left(\int_{\mathbb{R}^N\times\mathbb{R}^N}\Big(\frac{d(x,y)}{|x-y|}\Big)^p|f(x)-f(y)|^p \frac{dxdy}{d(x,y)^{2N}}\right)^{\frac1p}.$$
Using Theorem \ref{sobolev comparison theorem}, we conclude that
$$\|F\|_{L_p(X\times X,H^2(\mu\times\mu))}\leq c_{R,k}\|f\|_{\dot{W}^{\frac{N}{p},p}(\mathbb{R}^N)}.$$
Obviously, the integral kernel of the operator $[R_{\D,j},M_f]$ is $FH.$ The assertion follows now from Lemma \ref{s4 second lemma}.
\end{proof}

\section{Expected dyadic result}

{\color{red} I expect the following result to be true.}

\begin{theorem} If
$$\Big\{\int_Q\frac{|f|(y)dy}{|y_N|^N}\Big\}_{\substack{Q\in\mathcal{D}\\ |(c_Q)_N|\geq c\cdot s(Q)}}\in l_p$$
for some $c>0,$ then
$$\int_{\mathbb{R}^N}\frac{|f|^p(y)dy}{|y_N|^N}dx<\infty.$$
\end{theorem}



It is immediate that
$$\Big\{\int_{3Q}\frac{|f|(y)dy}{|y_N|^N}\Big\}_{\substack{Q\in\mathcal{D}\\ |(c_Q)_N|\geq (c+1)\cdot s(Q)}}\in l_p.$$

Let $x\in Q$ and let $t\in(\frac{s(Q)}{2},s(Q)).$ We have
$$B(x,t)=x+B(0,t)\subset x+B(0,s(Q))\subset x+[-s(Q),s(Q)]^d\subset $$
$$\subset Q+[-s(Q),s(Q)]^d=3Q.$$
Thus,
$$\int_{B(x,t)}\frac{|f|(y)dy}{|y_N|^N}\leq \int_{3Q}\frac{|f|(y)dy}{|y_N|^N}.$$


Set
$$h(x,t)=\int_{B(x,t)}\frac{|f|(y)dy}{|y_N|^N},\quad x\in [0,1]^N,\quad t\in(0,1).$$
So, we have
$$\sup_{\substack{x\in Q\\ t\in(\frac{s(Q)}{2},s(Q))}}h(x,t)\leq \int_{3Q}\frac{|f|(y)dy}{|y_N|^N}.$$
In particular,
$$\int_{Q\times(\frac{s(Q)}{2},s(Q))}h^p(x,t)\frac{dxdt}{t^{N+1}}\leq 2^N\Big(\int_{3Q}|f|\Big)^p.$$
Consequently,
$$\sum_{\substack{Q\in\mathcal{D}\\ |(c_Q)_N|\geq (c+1)\cdot s(Q)}}\int_{Q\times(\frac{s(Q)}{2},s(Q))}h^p(x,t)\frac{dxdt}{t^{N+1}}\leq$$
$$\leq 2^N\sum_{\substack{Q\in\mathcal{D}\\ |(c_Q)_N|\geq (c+1)\cdot s(Q)}}\Big(\int_{3Q}\frac{|f|(y)dy}{|y_N|^N}\Big)^p.$$

Suppose $(x,t)\in \mathbb{R}^N\times\mathbb{R}_+$ is such that $|x_N|\geq (2c+4)t.$ Find $m\in\mathbb{Z}$ such that $t\in(2^{-m-1},2^{-m}].$ Find $Q\in\mathcal{D}$ such that $s(Q)=2^{-m}$ and such that $x\in Q.$ We have
$$|(c_Q)_N|\geq |x_N|-|(x-c_Q)_N|\geq |x_N|-s(Q)\geq |x_N|-2t\geq$$
$$\geq (2c+4)t-2t=(2c+2)t\geq (c+1)s(Q).$$ 
Hence,
$$\int_{\substack{(x,t)\in \mathbb{R}^N\times\mathbb{R}_+\\ |x_N|\geq (2c+4)t}}h^p(x,t)\frac{dxdt}{t^{N+1}}\leq 2^N\sum_{\substack{Q\in\mathcal{D}\\ Q\subset Q_0\\ |(c_Q)_N|\geq (c+1)\cdot s(Q)}}\Big(\int_{3Q}\frac{|f|(y)dy}{|y_N|^N}\Big)^p.$$
So,
$$\int_{\substack{(x,t)\in \mathbb{R}^N\times\mathbb{R}_+\\ |x_N|\geq (2c+4)t}}h^p(x,t)\frac{dxdt}{t^{N+1}}<\infty.$$
Furthermore, it is easy to see that {\color{red} do we need details here?}
$$\int_{\substack{x\in\mathbb{R}^N\\ t\in\mathbb{R}_+\\ |x_N|\geq t}}h^p(x,t)\frac{dxdt}{t^{N+1}}<\infty.$$

{\color{red} from this, we should be able to derive that our weighted $L_p$-norm is finite.}

\section{Simplified version of Zhijie's argument}

\begin{lemma}\label{from rs lemma} Let $\mathcal{D}$ be the standard system of dyadic cubes. Let $O_v\in O(N,\mathbb{R})$ be such $O_ve_N=v$ and let $g\in G.$ We have
$$\Big\|\Big\{w(O_vQ)^{-1}\langle T\chi_{gO_vQ},\chi_{O_vQ}\rangle_{L_2(\mathbb{R}^N,w(x)dx)}\Big\}_{Q\in\mathcal{D}}\Big\|_{l_p(\mathcal{D})}\leq c_{g,v}\|T\|_{\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx))}.$$
\end{lemma}
\begin{proof} {\color{red} to be done later}.
\end{proof}

It is decidedly inconvenient that integral kernel $K_j$ of the operator $R_{{\rm Dunkl},j}$ is not positive. 
\begin{lemma}\label{dh lemma} Integral kernel of $R_{{\rm Dunkl},j}$ has the same sign as $(x-y)_j.$
\end{lemma}
\begin{proof} This should follow from Lemma 3.3 in {\color{red} refer to Dziubanski-Hejna paper Ji refers to}.
\end{proof}

Consider the operator $B_j$ {\color{red} I do not know whether this operator is bounded} whose integral kernel is given by the formula
$$(x,y)\to |K_j|(x,y),\quad x,y\in\mathbb{R}^N.$$

\begin{lemma}\label{from schur lemma} Suppose $M_{f\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{g\Gamma}}\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)).$ It follows that
$$M_{|f|\chi_{\Gamma}}B_jM_{\chi_{g\Gamma}}\in \mathcal{L}_p(L_2(\Gamma,w(x)dx))$$
\end{lemma}
\begin{proof} Multiplying by ${\rm sgn}(f)$ on the left, we may assume without loss of generality that $f\geq0.$ By Lemma \ref{dh lemma}, $K_j(x,y)$ has the same sign as $(x-y)_j.$ Thus,
$$M_{f\chi_{\Gamma}}B_jM_{\chi_{g\Gamma}}=\mathfrak{S}_{{\rm sgn}((x-y)_j)}(M_{f\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{g\Gamma}}).$$
Schur multiplier $\mathfrak{S}_{{\rm sgn}((x-y)_j)}$ is easily seen to be bounded on $\mathcal{L}_p(L_2(\Gamma,w)).$ This completes the proof.
\end{proof}

Let $g\in\Gamma$ be a reflection with respect to vector $v_0\in R.$

\begin{lemma}\label{for zhijie auxiliary estimates} Let $Q_0\in\mathcal{D}$ be such that
\begin{enumerate}[{\rm (i)}]
\item $Q_0\subset O_{v_0}^{-1}\Gamma$ touches the plane $\{x_N=0\}.$
\item $O_{v_0}Q_0$ is separated from any plane $\{\langle x,v\rangle=0\},$ $v_0\neq v\in R.$
\end{enumerate}
If $Q\in\mathcal{D}$ is such that $Q\subset Q_0$ and\footnote{Here, we use the shorthand
$${\rm const}_{Q_0}=\frac1{2N^{\frac12}}\min\{\inf_{\substack{x\in Q_0\\ v_0\neq v\in R}}|\langle x,v\rangle|,\inf_{\substack{x\in Q_0\\ v_0\neq v\in R}}|\langle gx,v\rangle|\}.$$}
$$|(c_Q)_N|\geq 2N^{\frac12}s(Q),\quad s(Q)\leq {\rm const}_{Q_0}.$$
\begin{enumerate}[{\rm (i)}]
\item\label{fzhpre} We have
$$|\langle O_{v_0}c_Q,v\rangle|\geq 2N^{\frac12}s(Q),\quad |\langle gO_{v_0}c_Q,v\rangle|\geq 2N^{\frac12}s(Q),\quad v\in\mathbb{R}.$$
\item\label{fzha} If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then
$$\frac12|\langle O_{v_0}c_Q,v\rangle|\leq|\langle x,v\rangle|\leq\frac32|\langle O_{v_0}c_Q,v\rangle|,$$
$$\frac12|\langle gO_{v_0}c_Q,v\rangle|\leq|\langle y,v\rangle|\leq\frac32|\langle gO_{v_0}c_Q,v\rangle|.$$
\item\label{fzhb} If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then
$$c_k^{-1}w(O_{v_0}c_Q)\leq w(x),w(y)\leq c_kw(O_{v_0}c_Q).$$
\item\label{fzhc} If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then
$$|(c_Q)_N|\leq |x-y|\leq 3|(c_Q)_N|.$$
\item\label{fzhd} If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then
$$c_k^{-1}|(c_Q)_N|^N\cdot w(O_{v_0}c_Q)\leq w(B(x,|x-y|))\leq c_k|(c_Q)_N|^N\cdot w(O_{v_0}c_Q).$$
\item\label{fzhe} We have
$$c_k^{-1}m(Q)w(O_{v_0}Q)\leq w(O_{v_0}Q)\leq c_km(Q)w(O_{v_0}Q).$$
\end{enumerate}
\end{lemma}
\begin{proof} Since $c_Q\in Q_0,$ it follows that
$$|\langle O_{v_0}c_Q,v\rangle|\geq \inf_{z\in Q_0}|\langle O_{v_0}z,v|\geq 2N^{\frac12}{\rm const}_{Q_0}\geq 2N^{\frac12}s(Q),\quad v_0\neq v\in R,$$
$$|\langle gO_{v_0}c_Q,v\rangle|\geq \inf_{z\in Q_0}|\langle gO_{v_0}z,v|\geq 2N^{\frac12}{\rm const}_{Q_0}\geq 2N^{\frac12}s(Q),\quad v_0\neq v\in R.$$
On the other hand, we have
$$|\langle gO_{v_0}c_Q,v_0\rangle|=|\langle O_{v_0}c_Q,v_0\rangle|=|\langle O_{v_0}c_Q,O_{v_0}e_N\rangle|=|\langle c_Q,e_N\rangle|=|(c_Q)_N|.$$
This yields \eqref{fzhpre}.

If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then
$$|\langle x,v\rangle|\geq|\langle O_{v_0}c_Q,v\rangle|-|\langle (x-O_{v_0}c_Q),v\rangle|\geq |\langle O_{v_0}c_Q,v\rangle|-|x-O_{v_0}c_Q|\geq$$
$$\geq |\langle O_{v_0}c_Q,v\rangle|-N^{\frac12}s(Q)\geq \frac12|\langle O_{v_0}c_Q,v\rangle|,\quad x\in O_vQ.$$
The other inequalities in \eqref{fzha} are proved similarly.

The assertion \eqref{fzhb} follows immediately from \eqref{fzha}.

If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then
$$|x-y|\leq |x-O_{v_0}c_Q|+|y-gO_{v_0}c_Q|+|O_{v_0}c_Q-gO_{v_0}c_Q|\leq$$
$$\leq 2N^{\frac12}s(Q)+|O_{v_0}c_Q-(O_{v_0}c_Q-2\langle O_{v_0}c_Q,v_0\rangle v_0)|=$$
$$=2N^{\frac12}s(Q)+2|\langle O_{v_0}c_Q,v_0\rangle|\leq 3|\langle O_{v_0}c_Q,v_0\rangle|=3|(c_Q)_N|.$$
The other inequality in \eqref{fzhc} is proved similarly.

Note that
$$w(B(x,|x-y|))\geq c_k^{-1}|x-y|^N\cdot w(x).$$
If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then the it follows from \eqref{fzhb} and \eqref{fzhc} that
$$w(B(x,|x-y|))\geq c_k^{-1}|\langle O_{v_0}c_Q,v_0\rangle|^N\cdot w(O_{v_0}c_Q).$$
Thus, we obtained the left hand side estimate in \eqref{fzhd}.

Note that
$$w(B(x,|x-y|))\leq c_k|x-y|^N\cdot\prod_{v\in R}(|x-y|+|\langle x,v\rangle|)^{k_v}.$$
If $x\in O_{v_0}Q$ and $y\in gO_{v_0}Q,$ then it follows from \eqref{fzha} and \eqref{fzhc} that
$$w(B(x,|x-y|))\leq c_k|\langle O_{v_0}c_Q,v_0\rangle|^N\cdot \prod_{v\in R}(|\langle O_{v_0}c_Q,v_0\rangle|+|\langle O_{v_0}c_Q,v\rangle|)^{k_v}.$$
By assumption on $Q_0,$
$$|\langle z,v_0\rangle|\leq {\rm const}'_{Q_0}|\langle z,v\rangle|,\quad z\in Q_0,\quad v\in R.$$
Since $O_{v_0}c_Q\in Q_0,$ it follows that
$$|\langle O_{v_0}c_Q,v_0\rangle|\leq {\rm const}'_{Q_0}|\langle O_{v_0}c_Q,v\rangle|,\quad  v\in R.$$
Hence,
$$w(B(x,|x-y|))\leq c_k|\langle O_{v_0}c_Q,v_0\rangle|^N\cdot \prod_{v\in R}((1+{\rm const}'_{Q_0})|\langle O_{v_0}c_Q,v\rangle|)^{k_v}.$$
Thus, we obtained the right hand side estimate in \eqref{fzhd}.

Finally, the assertion \eqref{fzhe} follows immediately from \eqref{fzhb}.
\end{proof}

\begin{lemma}\label{for zhijie extra estimate} Suppose that $v_0$ is not orthogonal to $e_j.$ Suppose $Q\in\mathcal{D}$ is such that
$$|(c_Q)_N|\cdot |(v_0)_j|\geq 2N^{\frac12}s(Q).$$
We have
$$\int_{gO_{v_0}Q}|(x-y)_j|dy\geq m(Q)\cdot |(c_Q)_N|\cdot |(v_0)_j|.$$
\end{lemma}
\begin{proof} Clearly,
$$\int_{gO_{v_0}Q}|(x-y)_j|dy\geq\max\Big\{\int_{gO_{v_0}Q}(x-y)_jdy,\int_{gO_{v_0}Q}(y-x)_jdy\Big\}.$$
Next,
$$\int_{gO_{v_0}Q}(x-y)_jdy=(x-gO_{v_0}c_Q)_j\cdot m(Q)$$
and
$$|(x-gO_{v_0}c_Q)_j|\geq |(O_{v_0}c_Q)_j-(gO_{v_0}c_Q)_j|-|(x-O_{v_0c_Q})_j|\geq$$
$$\geq |(O_{v_0}c_Q-gO_{v_0}c_Q)_j|-N^{\frac12}s(Q)=$$
$$= 2|\langle O_{v_0}c_Q,v_0\rangle|\cdot |(v_0)_j|-N^{\frac12}s(Q)\geq|\langle O_{v_0}c_Q,v_0\rangle|\cdot |(v_0)_j|.$$
Combining these inequalities, we complete the proof.
\end{proof}

\begin{lemma} Suppose $M_{f\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{g\Gamma}}\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)).$ Let $Q_0$ be as in Lemma \ref{for zhijie auxiliary estimates}. If $v_0$ is not orthogonal to $e_j,$ then
$$\Big\{\int_Q\frac{|f|(O_{v_0}^{-1}x)dx}{|x_N|^N}\Big\}_{\substack{Q\in\mathcal{D}\\ Q\subset Q_0\\ |(c_Q)_N|\geq c\cdot s(Q)}}\in l_p,\quad c=\frac{2N^{\frac12}}{|(v_0)_j|}.$$
\end{lemma}
\begin{proof} Combining Lemma \ref{from rs lemma} and Lemma \ref{from schur lemma}, we obtain
$$\Big\{w(O_{v_0}Q)^{-1}\langle |f|\cdot B_j(\chi_{gO_{v_0}Q}),\chi_{O_{v_0}Q}\rangle_{L_2(\mathbb{R}^N,w(x)dx)}\Big\}_{\substack{Q\in\mathcal{D}\\ O_{v_0}Q\subset\Gamma}}\in l_p.$$
In particular,
$$\Big\{w(O_{v_0}Q)^{-1}\langle |f|\cdot B_j(\chi_{gO_{v_0}Q}),\chi_{O_{v_0}Q}\rangle_{L_2(\mathbb{R}^N,w(x)dx)}\Big\}_{\substack{Q\in\mathcal{D}\\ Q\subset Q_0\\ s(Q)\leq {\rm const}_{Q_0}\\ |(c_Q)_N|\geq c\cdot s(Q)}}\in l_p.$$
Here, we use the shorthands
$${\rm const}_{Q_0}=\frac1{2N^{\frac12}}\min\{\inf_{\substack{x\in Q_0\\ v_0\neq v\in R}}|\langle x,v\rangle|,\inf_{\substack{x\in Q_0\\ v_0\neq v\in R}}|\langle gx,v\rangle|\},\quad c=\frac{2N^{\frac12}}{|(v_0)_j|}.$$


Using Lemmas \ref{nondegenerate}, \ref{for zhijie auxiliary estimates} and \ref{for zhijie extra estimate}, we write
$$w(O_{v_0}Q)^{-1}\langle |f|\cdot B_j(\chi_{gO_{v_0}Q}),\chi_{O_{v_0}Q}\rangle_{L_2(\mathbb{R}^N,w(x)dx)}=$$
$$=w(O_{v_0}Q)^{-1}\int_{O_{v_0}Q\times gO_{v_0}Q}|f|(x)|K_j|(x,y)w(x)w(y)dxdy\geq$$
$$\stackrel{L.\ref{nondegenerate}}{\geq} c_k^{(1)}w(O_{v_0}Q)^{-1}\int_{O_{v_0}Q\times gO_{v_0}Q}|f|(x)\frac{|(x-y)_j|}{|x-y|}\cdot\frac{w(x)w(y)}{w(B(x,|x-y|))}dxdy\geq$$
$$\stackrel{L.\ref{for zhijie auxiliary estimates} \eqref{fzhb}}{\geq} c_k^{(2)}w(O_{v_0}Q)^{-1}\int_{O_{v_0}Q\times gO_{v_0}Q}|f|(x)\frac{|(x-y)_j|}{|x-y|}\cdot\frac{w^2(O_{v_0}c_Q)}{w(B(x,|x-y|))}dxdy\geq$$
$$\stackrel{L.\ref{for zhijie auxiliary estimates} \eqref{fzhc}}{\geq} c_k^{(3)}w(O_{v_0}Q)^{-1}\int_{O_{v_0}Q\times gO_{v_0}Q}|f|(x)\frac{|(x-y)_j|}{|(c_Q)_N|}\cdot\frac{w^2(O_{v_0}c_Q)}{w(B(x,|x-y|))}dxdy\geq$$
$$\stackrel{L.\ref{for zhijie auxiliary estimates} \eqref{fzhe}}{\geq} c_k^{(4)}m(Q)^{-1}\int_{O_{v_0}Q\times gO_{v_0}Q}|f|(x)\frac{|(x-y)_j|}{|(c_Q)_N|}\cdot\frac{w(O_{v_0}c_Q)}{w(B(x,|x-y|))}dxdy\geq$$
$$\stackrel{L.\ref{for zhijie auxiliary estimates} \eqref{fzhd}}{\geq} c_{k,Q_0}^{(5)}m(Q)^{-1}\int_{O_{v_0}Q\times gO_{v_0}Q}|f|(x)|(x-y)_j|\cdot\frac1{|(c_Q)_N|^{N+1}}dxdy=$$
$$=c_{k,Q_0}^{(5)}\frac1{m(Q)\cdot |(c_Q)_N|^{N+1}}\int_{O_{v_0}Q}|f|(x)\Big(\int_{gO_{v_0}Q}|(x-y)_j|dy\Big)dx\geq$$
$$\stackrel{L.\ref{for zhijie extra estimate}}{\geq}c_{k,Q_0}^{(6)}\frac1{|(c_Q)_N|^N}\int_{O_{v_0}Q}|f|(x)dx\geq c_{k,Q_0}^{(7)}\int_Q\frac{|f|(O_{v_0}^{-1}x)dx}{|x_N|^N}.$$

As the condition $s(Q)\leq {\rm const}_{Q_0}$ excludes only finitely many terms, we can drop this condition.
\end{proof}

{\color{red} In case $v_0$ is orthogonal to $e_j,$ we similarly obtain
$$\Big\{\frac1{|(c_Q)_N|^{N+1}}\int_{O_{v_0}Q}|f|(x)|(x-O_{v_0}c_Q)_j|dx\Big\}_{Q\in\mathcal{D}_{v_0,Q_0}}\in l_p.$$
Of course, in this case, we should omit $|(v_0)_j|$ from the definition of $\mathcal{D}_{v_0,Q_0}.$
}


\section{Proof of Theorem \ref{main theorem} \eqref{mtb}}
\setcounter{equation}{0}

\begin{lemma}\label{for reverse holder first lemma} Let $\Gamma$ be a chamber and let $x_0\in\Gamma.$ We have
$$\sup_{x\in B(x_0,r)\cap\Gamma}w(x)\leq \frac{c_{k,\Gamma}}{m(B(x_0,r)\cap\Gamma)}\int_{B(x_0,r)\cap\Gamma}w(x)dx.$$
\end{lemma}
\begin{proof} If $x=x_0+y,$ $y\in B(0,r),$ then
$$|\langle x,v\rangle|\leq |\langle x_0,v\rangle|+r|\langle y,v\rangle|\leq |\langle x_0,v\rangle|+r\sqrt{2}.$$
Thus,
$$w(x)=\prod_{v\in R}(|\langle x_0,v\rangle|+|\langle y,v\rangle|)^{\epsilon\cdot k(v)}\leq\prod_{v\in R}(|\langle x_0,v\rangle|+r\sqrt{2})^{k(v)}.$$
In particular,
$$LHS\leq \prod_{v\in R}(|\langle x_0,v\rangle|+r\sqrt{2})^{k(v)}.$$

By convexity, $x_0+\Gamma\subset\Gamma.$ Hence,
$$x_0+(B(0,r)\cap\Gamma)\subset B(x_0,r)\cap\Gamma.$$
So,
$$\int_{B(x_0,r)\cap\Gamma}w(x)dx\geq\int_{x_0+(B(0,r)\cap\Gamma)}w(x)dx=\int_{B(0,r)\cap\Gamma}w(x_0+y)dy.$$
If $y\in\Gamma,$ then
$$|\langle x_0+y,v\rangle|=|\langle x_0,v\rangle|+|\langle y,v\rangle|,\quad v\in R.$$
Thus,
$$w(x_0+y)=\prod_{v\in R}(|\langle x_0,v\rangle|+|\langle y,v\rangle|)^{k(v)}\geq$$
$$\geq\max\{\prod_{v\in R}|\langle x_0,v\rangle|^{k(v)},\prod_{v\in R}|\langle y,v\rangle|^{k(v)}\}.$$
Hence,
$$\int_{B(x_0,r)\cap\Gamma}w(x)dx\geq\int_{B(0,r)\cap\Gamma}w(x_0+y)dy\geq$$
$$\geq\max\Big\{\int_{B(0,r)\cap\Gamma}\prod_{v\in R}|\langle x_0,v\rangle|^{k(v)}dy,\int_{B(0,r)\cap\Gamma}\prod_{v\in R}|\langle y,v\rangle|^{k(v)}dy\Big\}.$$
Obviously,
$$\int_{B(0,r)\cap\Gamma}\prod_{v\in R}|\langle x_0,v\rangle|^{k(v)}dy=r^N\prod_{v\in R}|\langle x_0,v\rangle|^{k(v)}\int_{B(0,1)\cap\Gamma}dy,$$
$$\int_{B(0,r)\cap\Gamma}\prod_{v\in R}|\langle y,v\rangle|^{k(v)}dy=r^{N+\sum_{v\in R}k(v)}\int_{B(0,1)\cap\Gamma}\prod_{v\in R}|\langle y,v\rangle|^{k(v)}dy.$$
Thus,
$$\int_{B(x_0,r)\cap\Gamma}w(x)dx\geq c_{k,\Gamma}^{(1)}\max\{r^N\prod_{v\in R}|\langle x_0,v\rangle|^{k(v)},r^{N+\sum_{v\in R}k(v)}\}.$$
Combining this with the preceding paragraph, we complete the proof.
\end{proof}

\begin{lemma}\label{for reverse holder second lemma} Let $\Gamma$ be a chamber and let $x_0\in\Gamma.$ Suppose $1<t<1+\frac1{|R|\cdot\max_{v\in R}k(v)}.$ For every $r>0,$ we have
$$\Big(\frac{\int_{B(x_0,r)\cap\Gamma}w^{1-t}(x)dx}{\int_{B(x_0,r)\cap\Gamma}w(x)dx}\Big)^{\frac1t}\leq c_{t,k,\Gamma} \frac{m(B(x_0,r)\cap\Gamma)}{\int_{B(x_0,r)\cap\Gamma}w(x)dx}.$$
\end{lemma}
\begin{proof} If $v\in R$ is such that
$$2\sqrt{2}r\leq |\langle x_0,v\rangle|,$$
then
$$|\langle x,v\rangle|\geq |\langle x_0,v\rangle|-|\langle x-x_0,v\rangle|\geq |\langle x_0,v\rangle|-r\sqrt{2}\geq \frac12|\langle x_0,v\rangle|,\quad x\in B(x_0,r).$$
In this case,
$$\int_{B(x_0,r)}|\langle x,v\rangle|^{(1-t)|R|k(v)}dx\leq 2^{(t-1)|R|k(v)}|\langle x_0,v\rangle|^{(1-t)|R|k(v)}\cdot\int_{B(x_0,r)}dx=$$
$$=2^{(t-1)|R|k(v)}|\langle x_0,v\rangle|^{(1-t)|R|k(v)}\cdot {\rm Vol}(\mathbb{B}^N)r^N.$$

If $v\in R$ is such that
$$|\langle x_0,v\rangle|\leq 2\sqrt{2}r,$$
then let $y_0=\langle x_0,v\rangle$ and $z_0=x_0-\frac12y_0v_0.$ Note that $|y_0|\leq 2\sqrt{2}r.$ Using new variables $y=\langle x,v\rangle$ and $z=x-\frac12yv,$ we write
$$\int_{B(x_0,r)}|\langle x,v\rangle|^{(1-t)|R|k(v)}dx=\int_{|y-y_0|^2+|z-z_0|^2\leq r^2}|y|^{(1-t)|R|k(v)}dydz\leq$$
$$\leq \int_{|y-y_0|^2,|z-z_0|^2\leq r^2}|y|^{(1-t)|R|k(v)}dydz=$$
$$=r^{N-1}{\rm Vol}(\mathbb{B}^{N-1})\cdot\int_{|y-y_0|\leq r}|y|^{(1-t)|R|k(v)}dy\leq$$
$$=r^{N-1}{\rm Vol}(\mathbb{B}^{N-1})\cdot\int_{|y|\leq (1+2\sqrt{2}) r}|y|^{(1-t)|R|k(v)}dy=$$
$$=r^{N+(1-t)|R|k(v)}{\rm Vol}(\mathbb{B}^{N-1})\cdot\int_{|y|\leq 1+2\sqrt{2}}|y|^{(1-t)|R|k(v)}dy.$$

Combining the estimates in the preceding paragraphs, we write
$$\int_{B(x_0,r)}|\langle x,v\rangle|^{(1-t)|R|k(v)}dx\leq c_1R^n\big(\max\{|\langle x_0,v\rangle|,r\}\big)^{(1-t)|R|k(v)}.$$
By H\"older inequality,
$$\int_{B(x_0,r)}w^{1-t}(x)dx\leq \prod_{v\in R}\Big(\int_{B(x_0,r)}|\langle x,v\rangle|^{(1-t)|R|k(v)}dx\Big)^{\frac1{|R|}}\leq$$
$$\leq c_2R^N\prod_{v\in R}\big(\max\{|\langle x_0,v\rangle|,r\}\big)^{(1-t)k(v)}.$$
The assertion follows easily.
\end{proof}

\begin{lemma}\label{hytonen verification lemma} Let $\Gamma$ be a chamber equipped with Euclidean distance. Measures $dx$ and $w(x)dx$ and operator $T_{\Gamma,j}=M_{\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{\Gamma}}$ satisfy {\color{red} on $\Gamma$} the conditions in Theorem 1.4 in \cite{Hytonen-new}.
\end{lemma}
\begin{proof} {\bf Step 1:} We claim that measures $dx$ and $w(x)dx$ are $A_{\infty}$-equivalent (in the sense of Proposition 1.2 in \cite{Hytonen-new}). This verifies Assumption (i) in Theorem 1.4 in \cite{Hytonen-new}.

Let $x_0\in\Gamma.$ By Lemmas \ref{for reverse holder first lemma} and \ref{for reverse holder second lemma},
$$\Big(\frac1{m(B\cap\Gamma)}\int_{B\cap\Gamma}w^t(x)dx\Big)^{\frac1t}\leq\frac{c_{k,\Gamma}}{m(B\cap\Gamma)}\int_{B\cap\Gamma}w(x)dx,\quad t>0.$$
$$\Big(\frac{\int_{B\cap\Gamma}w^{1-t}(x)dx}{\int_{B\cap\Gamma}w(x)dx}\Big)^{\frac1t}\leq c_G \frac{m(B\cap\Gamma)}{\int_{B\cap\Gamma}w(x)dx},\quad 1<t<1+\frac1{|R|\cdot\max_{v\in R}k(v)}.$$
The assertion follows now from Proposition 3.6 in \cite{Hytonen-new}.

{\bf Step 2:} Assumption (ii) in Theorem 1.4 in \cite{Hytonen-new} is obvious.

{\bf Step 3:} Integral kernel $K_j$ of the operator $T_{\Gamma,j}$ satisfies the estimates
$$|K_j(x,y)|\leq\frac{c_G}{w(B(x,|x-y|)\cap \Gamma)},\quad x,y\in\Gamma,$$
$$|K_j(x,y)-K_j(x',y)|\leq \frac{|x-x'|}{|x-y|}\cdot\frac{c_G}{w(B(x,|x-y|))},\quad |x-x'|\leq\frac12|x-y|,\quad x,x',y\in\Gamma,$$
$$|K_j(x,y)-K_j(x,y')|\leq \frac{|y-y'|}{|x-y|}\cdot\frac{c_G}{w(B(x,|x-y|))},\quad |y-y'|\leq\frac12|x-y|,\quad x,y,y'\in\Gamma.$$ 
In other words, $K_j$ is a standard kernel for the measure $w(x)dx.$

This follows from Theorem 1.1 in \cite{MR4733961}.

{\bf Step 4:} Kernel $K_j$ is non-degenerate in the following sense: for every $x\in\Gamma$ and $r>0,$ there exists $y\in\Gamma$ with $|x-y|=r$ and
$$|K_j(x,y)|\geq\frac{c_G}{w(B(x,|x-y|)\cap \Gamma)}.$$

Let 
$$c_{\Gamma}=(\max_{z\in\Gamma,\ |z|=1}|z_j|)^{-1}.$$
Choose $z\in\Gamma$ such that $|z_j|=c_{\Gamma}^{-1}$ and $|z|=1.$ Set $y=x+rz.$ Since $x,z\in\Gamma,$ it follows that $y\in\Gamma.$ Clearly, $|(x-y)_j|\geq rc_{\Gamma}^{-1}$ and $|x-y|=r.$  The claim follows from {\color{red} the proof of the} Theorem 1.2 in \cite{MR4733961}.

{\bf Step 4:} Lebesgue measure on $\Gamma$ is, obviously, $N$-regular and possesses the $(1,1)$ Poincare inequality.
\end{proof}

\begin{lemma}\label{chamber sobolev lemma} If $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)),$ then $f\in\dot{W}^{\frac{N}{p},p}(\Gamma)$ for every chamber $\Gamma.$ Furthermore,
$$\|f\|_{\dot{W}^{\frac{N}{p},p}(\Gamma)}\leq c_{R,k}\|[R_{{\rm Dunkl},j},M_f]\|_{\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx))}.$$
\end{lemma}
\begin{proof} This follows immediately from Lemma \ref{hytonen verification lemma} and Theorem 1.4 in \cite{Hytonen-new}.
\end{proof}

Let $U_g:L_2(\mathbb{R}^N,w(x)dx)\to L_2(\mathbb{R}^N,w(x)dx)$ be given by the formula $U_g\xi=\xi\circ g^{-1}.$ Denote for brevity
$$A_{g,\Gamma,j}=M_{\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{g\Gamma}}U_g.$$

\begin{lemma}\label{different chambers cwikel condition} If $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)),$ then\footnote{{\color{red} this footnote is temporary, for the authors' use only. the combination of assertions of Lemmas \ref{chamber sobolev lemma} and \ref{different chambers cwikel condition} yield $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)).$}}
$$M_{(f-f\circ g)\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{g\Gamma}}\in \mathcal{L}_p(L_2(\Gamma,w(x)dx))$$
for every $g\in G$ and for every chamber $\Gamma.$ Furthermore,
$$\|M_{(f-f\circ g)\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{g\Gamma}}\|_{ \mathcal{L}_p(L_2(\Gamma,w(x)dx))}\leq c_{R,k}\|[R_{{\rm Dunkl},j},M_f]\|_{\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx))}.$$
\end{lemma}
\begin{proof} By assumption, 
$$M_{\chi_{\Gamma}}[R_{{\rm Dunkl},j},M_f]M_{\chi_{g\Gamma}}U_g\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx)).$$
We, therefore, have
\begin{equation}\label{dccc eq0}
A_{g,\Gamma,j}M_{f\circ g}-M_fA_{g,\Gamma,j}\in\mathcal{L}_p(L_2(\Gamma,w(x)dx)).
\end{equation}

Let us denote by $K_{g,\Gamma,j}$ the integral kernel of the operator $A_{g,\Gamma,j}.$ By Lemma \ref{kernelestimate},
$$|K_{g,\Gamma,j}|(x,y)\leq\frac{c_G}{w(B(x,|x-y|))},\quad x,y\in\Gamma.$$
We know from Lemma \ref{chamber sobolev lemma} that $f\in\dot{W}^{\frac{N}{p},p}(\Gamma).$ We now infer from the Proposition \ref{upper bound} that 
\begin{equation}\label{dccc eq1}
A_{g,\Gamma,j}M_{f\circ g}-M_{f\circ g}A_{g,\Gamma,j}\in\mathcal{L}_p(L_2(\Gamma,w(x)dx)).
\end{equation}

Combining \eqref{dccc eq0} and \eqref{dccc eq1}, we write
$$M_{f-f\circ g}A_{g,\Gamma,j}\in\mathcal{L}_p(L_2(\Gamma,w(x)dx)).$$
Multiplying by $U_g^{-1}$ on the right, we complete the proof.
\end{proof}

\section{Useful part of Zhijie argument}

\begin{lemma}\label{constructHPQ}
 Assume that $P$ and $Q$ are two $w$-measurable sets on $\mathbb{R}^N$ such that $P\subset Q$. Suppose that there exist constants $1<A<B<\infty$ such that
\begin{align}\label{twosidecond}
  Aw(P)\leq w(Q)\leq Bw(P).
\end{align}
There is a $w$-measurable  function $H_{P,Q}$ on $\mathbb{R}^N$ satisfying
\begin{enumerate}
  \item\label{111} $|H_{P,Q}(x)|\leq w(Q)^{-1/2}\chi_Q(x)$;\smallskip
  \item\label{222} $\int_Q H_{P,Q}(x)dw(x)=0$;\smallskip
  \item\label{333} $|(b)_P-(b)_Q|\approx w(Q)^{-1/2}|\langle b,H_{P,Q}\rangle_{L^2(\mathbb{R}^N,dw)}|$, where the implicit constants depending only on $A$ and $B$.
\end{enumerate}
\end{lemma}
\begin{proof}
Denote by $c_{A,B}:=\min\left\{(A-1)^{1/2},(B-1)^{-1/2}\right\}$ and let
\begin{align*}
H_{P,Q}:=c_{A,B}\left(\frac{w(Q\backslash P)^{1/2}}{w(P)^{1/2}w(Q)^{1/2}}\chi_P(x)-\frac{w(P)^{1/2}}{w(Q)^{1/2}w(Q\backslash P)^{1/2}}\chi_{Q\backslash P}(x)\right).
\end{align*}
Now we verify the function $H_{P,Q}$ satisfies all the required properties.

$\bullet $ {\bf Verification of \eqref{111}.}

It is obvious that $\supp H_{P,Q}\subset Q$. Next, we apply inequality \eqref{twosidecond} to see that
\begin{align}\label{twosdde}
(A-1)w(P)\leq w(Q\backslash P)=w(Q)-w(P)\leq (B-1)w(P).
\end{align}
Thus, $c_{A,B}w(Q\backslash P)^{1/2}\leq w(P)^{1/2}$ and $c_{A,B}w(P)^{1/2}\leq w(Q\backslash P)^{1/2}.$  This verifies property \eqref{111}.

$\bullet $ {\bf Verification of \eqref{222}.}

Note that
\begin{align*}
\int_Q H_{P,Q}(x)dw(x)&=c_{A,B}\int_Q \left(\frac{w(Q\backslash P)^{1/2}}{w(P)^{1/2}w(Q)^{1/2}}\chi_P(x)-\frac{w(P)^{1/2}}{w(Q)^{1/2}w(Q\backslash P)^{1/2}}\chi_{Q\backslash P}(x)\right)dw(x)\\
&=c_{A,B}\left(\frac{w(Q\backslash P)^{1/2}w(P)^{1/2}}{w(Q)^{1/2}}-\frac{w(P)^{1/2}w(Q\backslash P)^{1/2}}{w(Q)^{1/2}}\right)\\
&=0.
\end{align*}
This verifies property \eqref{222}.

$\bullet $ {\bf Verification of \eqref{333}.}

We first show a stronger equality, that is,
\begin{align}\label{strongequality}
c_{A,B}\big((b)_P-(b)_Q\big)=\langle b,H_{P,Q}\rangle_{L^2(\mathbb{R}^N,dw)} \frac{w(Q\backslash P)^{1/2}}{w(P)^{1/2}w(Q)^{1/2}}.
\end{align}
Indeed, we have
\begin{align*}
&{\rm RHS}\ {\rm of}\ \eqref{strongequality}\\
&=c_{A,B}\left(\frac{w(Q)-w(P)}{w(P)w(Q)}\int_P b(x)dw(x)-\frac{1}{w(Q)}\int_{Q\backslash P}b(x)dw(x)\right)\\
&=c_{A,B}\left(\fint_Pb(x)dw(x)-\frac{1}{w(Q)}\int_Pb(x)dw(x)-\bigg(\fint_Qb(x)dw(x)-\frac{1}{w(Q)}\int_Pb(x)dw(x)\bigg)\right)\\
&={\rm LHS}\ {\rm of}\ \eqref{strongequality}.
\end{align*}
Combining \eqref{strongequality} with \eqref{twosdde}, we deduce property \eqref{333} and then, Lemma \ref{constructHPQ}.
\end{proof}

\begin{lemma}\label{NWOlemma}
Let $b$ and $H$ be two $w$-measurable complex-valued functions on $\mathbb{R}^N$. Assume that there is a cube ${\displaystyle Q\in \bigcup_{\nu=1}^{2^N}\mathbb{D}^\nu}$ such that
\begin{enumerate}
  \item ${\displaystyle|H(x)|\leq w(Q)^{-1/2}\chi_Q\ { for}\ { all}\  x\in\mathbb{R}^N;}$
  \item
${\displaystyle\int_{Q}H(x)dw(x)=0}$.
\end{enumerate}
There exist function sequences $\{e_{Q,s}\}_{s=1}^4$ and $\{f_{Q,s}\}_{s=1}^4$ such that
$$|e_{Q,s}|+|f_{Q,s}|\leq w(Q)^{-1/2}\chi_{6\sqrt{N}Q},$$ and that
\begin{align}\label{yieldsss}
  |\langle b,H\rangle_{L^2(\mathbb{R}^N,dw)}|&\lesssim w(Q)^{1/2}\sum_{s=1}^4|\langle [b,R_{\D,j}]e_{Q,s},f_{Q,s}\rangle_{L^2(\mathbb{R}^N,dw)}|.
\end{align}
\end{lemma}


{\color{red} THE MOST IMPORTANT TASK FOR ZHIJIE. please get rid of complex median method in this proof. our functions are WITHOUT LOSS OF GENERALITY real-valued.}

\begin{proof}
By Lemma \ref{nondegenerate}, for any $k\in\mathbb{Z}$ and $Q\in\mathbb{D}_k^\nu$, since $Q\subset B(c_Q,\sqrt{N}2^{-k-1})=:B_Q$,
there exists a ball $\widehat{B_Q}:=B(c_{\widehat{B_Q}},\sqrt{N}2^{-k-1})$ such that $\|c_Q-c_{\widehat{B_Q}}\|=5\sqrt{N}2^{-k-1}$, and that for every $(x,y)\in Q\times \widehat{B_Q}$, $K_{\D,j}(x,y)$ does not change sign and satisfies
 \begin{align}\label{nondegeneratelower}
  |K_{\D,j}(x,y)|\gtrsim\frac{1}{w(B_Q)}.
\end{align}

It follows from the doubling inequality \eqref{doublinginequality} that
$$w(B_Q)\sim w(\widehat{B_Q})\sim w(Q).$$

By \cite[Theorem 1.5]{Zhangwei2}, there exist $\theta\in [0,2\pi)$, depending on $\widehat{B_Q}$ and $b$, and $\alpha_{\widehat{B_Q}}(b)\in\mathbb{C}$ such that if we denote
$$F_s^Q:=\left\{x\in\widehat{B_Q}:\ -\frac{\pi}{4}+\frac{(s-1)\pi}{2}\leq \arg\Big(e^{i\theta}\big(\alpha_{\widehat{B_Q}}(b)-b(x)\big)\Big)\leq -\frac{\pi}{4}+\frac{s\pi}{2}\ or\ b(x)=\alpha_{\widehat{B_Q}}(b)\right\},$$
where $s\in\{1,2,3,4\}$, and $\arg(z)$ is the argument of a complex number $z$, then
$w(F_s^Q)\geq \frac{1}{16}w(\widehat{B_Q})$, and $F_1^Q\cup F_2^Q\cup F_3^Q\cup F_4^Q=\widehat{B_Q}$. $\alpha_{\widehat{B_Q}}(b)$ is called a complex median value of $b$ over $\widehat{B_Q}$.
Similarly, if we denote
\begin{align}
  E_s^Q:=\left\{x\in Q:\ -\frac{\pi}{4}+\frac{(s-1)\pi}{2}\leq \arg\Big(e^{i\theta}\big(b(x)-\alpha_{\widehat{B_Q}}(b)\big)\Big)\leq -\frac{\pi}{4}+\frac{s\pi}{2}\ or\ b(x)=\alpha_{\widehat{B_Q}}(b)\right\}
\end{align}
then $E_1^Q\cup E_2^Q\cup E_3^Q \cup E_4^Q= Q$.

It follows from the assumption of $H$ that
\begin{align*}
  |\langle b,H\rangle_{L^2(\mathbb{R}^N,dw)}|
  &=\Bigg|\int_Q b(x)H(x)dw(x)\Bigg|\nonumber\\
  &=\Bigg|\int_Q\big(b(x)-\alpha_{\widehat{B_Q}}(b)\big)H(x)dw(x)\Bigg|\nonumber\\
  &\lesssim w(Q)^{-\frac{1}{2}}\int_Q|b(x)-\alpha_{\widehat{B_Q}}(b)|dw(x)\nonumber\\
  &\lesssim w(Q)^{-\frac{1}{2}}\sum_{s=1}^4\int_{E_s^Q}|b(x)-\alpha_{\widehat{B_Q}}(b)|dw(x)=:\sum_{s=1}^4 M_s^Q.
\end{align*}
Note that for any $s\in\{1,2,3,4\},$ if $x\in E_s^Q$ and $y\in F_s^Q$, then
\begin{align}\label{complexmedian}
  |b(x)-\alpha_{\widehat{B_Q}}(b)|&\leq |e^{i\theta}(b(x)-\alpha_{\widehat{B_Q}}(b))|+|e^{i\theta}(\alpha_{\widehat{B_Q}}(b)-b(y))|\nonumber\\
  &\leq 2|e^{i\theta}(b(x)-b(y))|=2|b(x)-b(y)|.
\end{align}
Combining \eqref{nondegeneratelower} with \eqref{complexmedian}, we deduce that
\begin{align}
  M_s^Q&\lesssim w(Q)^{-\frac{1}{2}}\int_{E_s^Q}|b(x)-\alpha_{\widehat{B_Q}}(b)|dw(x)\cdot \frac{w(F_s^Q)}{w(Q)}\nonumber\\
  &\lesssim w(Q)^{-\frac{1}{2}}\int_{E_s^Q}\int_{F_s^Q}|b(x)-\alpha_{\widehat{B_Q}}(b)||K_{\D,j}(x,y)|dw(y)dw(x)\nonumber\\
  &\lesssim w(Q)^{-\frac{1}{2}}\int_{E_s^Q}\int_{F_s^Q}|b(x)-b(y)||K_{\D,j}(x,y)|dw(y)dw(x).
\end{align}
Moreover, note that for any $x\in E_1^Q$ and $y\in F_1^Q$, the arguments of $e^{i\theta}(b(x)-\alpha_{\widehat{B_Q}}(b))$ and $e^{i\theta}(\alpha_{\widehat{B_Q}}(b)-b(y))$ both belong to $[-\frac{\pi}{4},\frac{\pi}{4}]$. Thus,
$$-\frac{\pi}{4}\leq \arg\Big(e^{i\theta}\big(b(x)-b(y)\big)\Big)\leq \frac{\pi}{4}.$$
This implies that
\begin{align}
  |b(x)-b(y)|=\Big|e^{i\theta}\big(b(x)-b(y)\big)\Big|\leq 2{\rm Re}\Big(e^{i\theta}\big(b(x)-b(y)\big)\Big).
\end{align}
%and
%\begin{align}
%\Big|{\rm Im}\Big(e^{i\theta}(b(x)-b(y))\Big)\Big|\leq {\rm Re}\Big(e^{i\theta}\big(b(x)-b(y)\big)\Big).
%\end{align}
Therefore,
\begin{align*}
  M_1^Q
  &\lesssim w(Q)^{-\frac{1}{2}}\int_{E_1^Q}\int_{F_1^Q}{\rm Re}\Big(e^{i\theta}\big(b(x)-b(y)\big)\Big)K_{\D,j}(x,y)dw(y)dw(x)\nonumber\\
   &\lesssim w(Q)^{-\frac{1}{2}}\Bigg|\int_{E_1^Q}\int_{F_1^Q}e^{i\theta}\big(b(x)-b(y)\big)K_{\D,j}(x,y)dw(y)dw(x)\Bigg|\nonumber\\
   &= w(Q)^{-\frac{1}{2}}\Bigg|\int_{E_1^Q}\int_{F_1^Q}\big(b(x)-b(y)\big)K_{\D,j}(x,y)dw(y)dw(x)\Bigg|.
\end{align*}
The other three terms  $M_2^Q$, $M_3^Q$, $M_4^Q$ can be handled in the same way as $M_1^Q$ by rotation. Hence, for $s\in\{1,2,3,4\}$,
\begin{align}
  M_s^Q&\lesssim w(Q)^{-\frac{1}{2}}\Bigg|\int_{E_s^Q}\int_{F_s^Q}\big(b(x)-b(y)\big)K_{\D,j}(x,y)dw(y)dw(x)\Bigg|\nonumber\\
  &=w(Q)^{\frac{1}{2}}\Bigg\langle [b,R_{\D,j}]\frac{\chi_{F_s^Q}}{w(Q)^{1/2}},\frac{\chi_{E_s^Q}}{w(Q)^{1/2}}\Bigg\rangle_{L^2(\mathbb{R}^N,dw)}.
\end{align}
Letting
$$e_{Q,s}:=\frac{\chi_{F_s^Q}}{w(Q)^{1/2}}\  {\rm and}\  f_{Q,s}:=\frac{\chi_{E_s^Q}}{w(Q)^{1/2}},$$
which deduces \eqref{yieldsss}.
Clearly, $\supp E_s^Q\subset Q$. Moreover, since $\|c_Q-c_{\widehat{B_Q}}\|=5\sqrt{N}2^{-k-1}$, we see that $\supp F_s^Q\subset  6\sqrt{N}Q$. This completes the proof of Lemma \ref{NWOlemma}.
\end{proof}



With the collection $\{\mathbb{D}^\nu\}_{\nu=1}^{2^N}$ of dyadic cubes systems given in Lemma \ref{Nsystems}, we define the conditional expectation of a locally integrable function $f$ on $\mathbb{R}^N$ by the expression
$$E_k^\nu(f)(x):=\sum_{Q\in\mathbb{D}_k^\nu}(f)_Q\chi_Q(x),\ x\in\mathbb{R}^N.$$
Moreover, we define the martingale difference of $f$ by
\[
D_k^\nu (f):=E_{k+1}^\nu (f)-E_k^\nu (f).
\]

\begin{definition}
For any $1\leq p<\infty$ and $f\in L^1_{\rm loc}(\mathbb{R}^N,dw)$, we define the martingale Besov  (semi)norm of $f$ by
    $$\|f\|_{{MB}^p(\mathbb{R}^N,dw)}:=\sum_{\nu=1}^{2^N}\bigg(\sum_{k\in\mathbb{Z}}\int_{\mathbb{R}^{N}}\frac{|D_k^\nu f(x)|^p}{w(B(x,2^{-k}))}dw(x)\bigg)^{1/p}.$$
\end{definition}

\begin{lemma}\label{MartingaleBesov}
Suppose that $1<p<\infty$ and $b\in L^1_{\mathrm{loc}}(\mathbb{R}^N,dw)$. We have
\[
\|b\|_{B^p(\mathbb{R}^N,dw)}\lesssim \|b\|_{{MB}^p(\mathbb{R}^N,dw)}.
\]
\end{lemma}
\begin{proof}
We first decompose $\mathbb{R}^N\times \mathbb{R}^N$ into dyadic shells as follows.
\begin{align}\label{rhs1010}
  \int_{\mathbb{R}^N}\int_{\mathbb{R}^N}\frac{|b(x)-b(y)|^p}{w(B(x,\|x-y\|))^2}dw(x)dw(y)
  &\leq \sum_{k\in\mathbb{Z}}\iint_{\substack{2^{-k-1}<\|x-y\|\leq 2^{-k}\\x,y\in\mathbb{R}^N}}\frac{|b(x)-b(y)|^p}{w(B(x,\|x-y\|))^2}dw(x)dw(y)\nonumber\\
  &\leq \sum_{k\in\mathbb{Z}}\iint_{\substack{2^{-k-1}<\|x-y\|\leq 2^{-k}\\x,y\in\mathbb{R}^N}}\frac{|b(x)-b(y)|^p}{w(B(x,2^{-k-1}))^2}dw(x)dw(y).
\end{align}
Fix $k\in\mathbb{Z}$ and let $\mathbb{D}_k^0$ be the standard system of dyadic cubes over $\mathbb{R}^N$ at scale $2^{-k}$ (see Notation \ref{standardsystem}).
For each $Q\in\mathbb{D}_k^0$, one has
$$I_Q:=\left\{y\in\mathbb{R}^N: {\rm dist}(y,Q)\leq 2^{-k}\right\}\subset B(c_Q,\sqrt{N}2^{-k+1}),$$
where we used the notation ${\rm dist}(y,Q)$ to denote the Euclidean distance from a point $y\in\mathbb{R}^N$ to a set $Q$. By Lemma \ref{Nsystems}, there exists ${\displaystyle Q'\in \bigcup_{\nu=1}^{2^N}\mathbb{D}_{k-k_0}^\nu}$, where $k_0:=\lfloor \log_2(24\sqrt{N})\rfloor$, such that
$$B(c_Q,\sqrt{N}2^{-k+1})\subset Q'\subset B(c_Q,12N\cdot2^{-k+1}).$$
With these observations, we conclude that
\begin{align}\label{RHS1111}
  &{\rm RHS}\ {\rm of}\ \eqref{rhs1010}\nonumber\\
  &\leq \sum_{k\in\mathbb{Z}}\sum_{Q\in\mathbb{D}_k^0}\int_{Q}\int_{I_Q}\frac{|b(x)-b(y)|^p}{w(B(x,2^{-k-1}))^2}dw(x)dw(y)\nonumber\\
   &\leq \sum_{k\in\mathbb{Z}}\sum_{Q\in\mathbb{D}_k^0}\int_{Q'}\int_{Q'}\frac{|b(x)-b(y)|^p}{w(B(x,2^{-k-1}))^2}dw(x)dw(y)\nonumber\\
   &\leq \sum_{\nu=1}^{2^N}\sum_{k\in\mathbb{Z}}\sum_{Q\in\mathbb{D}_{k-k_0}^\nu}\int_{Q}\int_{Q}\frac{|b(x)-b(y)|^p}{w(B(x,2^{-k-1}))^2}dw(x)dw(y),
\end{align}
where in the last inequality we used the fact that each $Q'\in \mathbb{D}_{k-k_0}^\nu$ contains at most $c$ (an absolute constant) $Q\in\mathbb{D}_k^0$. By the doubling inequality \eqref{doublinginequality}, for any $x,y\in Q\in\mathbb{D}_{k-k_0}^\nu$,
$$w(Q)\sim w(B(x,2^{-k-1}))\sim w(B(y,2^{-k-1})).$$
Therefore, by inserting and subtracting the conditional expectation $E_{k-k_0}^\nu(b)$, we deduce that
\begin{align}
  &{\rm RHS}\ {\rm of}\ \eqref{RHS1111}\nonumber\\
  &\lesssim \sum_{\nu=1}^{2^N}\sum_{k\in\mathbb{Z}}\sum_{Q\in\mathbb{D}_{k-k_0}^\nu}\int_{Q}\int_{Q}\frac{|b(x)-E_{k-k_0}^\nu(b)(x)|^p}{w(B(x,2^{-k-1}))^2}+\frac{|b(y)-E_{k-k_0}^\nu(b)(y)|^p}{w(B(x,2^{-k-1}))^2}dw(x)dw(y)\nonumber\\
  &\lesssim\sum_{\nu=1}^{2^N}\sum_{k\in\mathbb{Z}}\sum_{Q\in\mathbb{D}_{k-k_0}^\nu}\int_{Q}\frac{|b(x)-E_{k-k_0}^\nu(b)(x)|^p}{w(B(x,2^{-k-1}))}dw(x)\nonumber\\
  &\lesssim\sum_{\nu=1}^{2^N}\sum_{k\in\mathbb{Z}}\int_{\mathbb{R}^N}\frac{|b(x)-E_{k}^\nu(b)(x)|^p}{w(B(x,2^{-k}))}dw(x),
\end{align}
where the last inequality used the change of index and the doubling inequality \eqref{doublinginequality}.

To proceed, we apply  Lemma~\ref{Haarexpansion} to write
\[
b(x)-E_k^\nu (b)(x)=\sum_{m=0}^{\infty}D_{k+m}^\nu (b)(x),
\]
 where the sum converges pointwise almost everywhere. Moreover, by the lower doubling inequality \eqref{doublinginequality},
\begin{align*}
C^{-1}2^{mN}\leq \frac{w(B(x,2^{-k}))}{w(B(x,2^{-(m+k)}))}.
\end{align*}
Combining these facts, we deduce that
\begin{align}\label{eq:A-k-new}
\Bigg(\int_{\mathbb{R}^N}\frac{|b(x)-E_k^\nu (b)(x)|^p}{w(B(x,2^{-k}))}\,dw(x)\Bigg)^{\!1/p}
&\le \sum_{m\ge0}
\Bigg(\int_{\mathbb{R}^N}\frac{|D_{k+m}^\nu (b)(x)|^p}{w(B(x,2^{-k}))}\,dw(x)\Bigg)^{\!1/p}\nonumber\\
&\lesssim \sum_{m\ge0}2^{-mN/p}\Bigg(\int_{\mathbb{R}^N}\frac{|D_{k+m}^\nu (b)(x)|^p}{w(B(x,2^{-(k+m)}))}\,dw(x)\Bigg)^{\!1/p}.
\end{align}
Since $\sum_{m\geq 0}2^{-mN/p}=(1-2^{-N/p})^{-1}<+\infty$, by discrete convolution Young's inequality, we have
\[
\Bigg(\sum_{k\in\mathbb{Z}}\int_{\mathbb{R}^N}\frac{|b(x)-E_k^\nu (b)(x)|^p}{w(B(x,2^{-k}))}\,dw(x)\Bigg)^{\!1/p}
\lesssim
\Bigg(\sum_{k\in\mathbb{Z}}\int_{\mathbb{R}^N}\frac{|D_{k}^\nu(b)(x)|^p}{w(B(x,2^{-k}))}\,dw(x)\Bigg)^{\!1/p}.
\]
Summing over $\nu$ yields the required assertion.
\end{proof}






\begin{lemma}\label{standard}
Suppose that $1< p<\infty$ and $b\in L^1_{\rm loc}(\mathbb{R}^N,dw)$. If $[b,R_{\D,j}]\in S^p$ for some $j\in\{1,2,\ldots,N\}$, then
\[
\|b\|_{B^p(\mathbb{R}^N,dw)}\lesssim \|[b,R_{\D,j}]\|_{S^p}.
\]
\end{lemma}
\begin{proof}
By Lemma \ref{MartingaleBesov}, it suffices to show that
for each $\nu\in\{1,2,\ldots,2^N\}$, we have
\begin{align}\label{martingaleassertion}
  \bigg(\sum_{k\in\mathbb{Z}}\int_{\mathbb{R}^N}\frac{|D_k^\nu(b)(x)|^p}{w(B(x,2^{-k}))}dw(x)\bigg)^{1/p}\lesssim\|[b,R_{\D,j}]\|_{S^p}.
\end{align}

  For any $Q\in\mathbb{D}_k^\nu$, we let $h_Q^1,h_Q^2,\ldots,h_Q^{2^N-1}$ be a family of Haar functions as defined in Section \ref{HaarSection}. Next, we choose $h_Q$ among these functions such that
  $$h_Q:=\left\{h_Q^i:\ \left|\int_Qb(x)h_Q^i(x)dw(x)\right|\ {\rm is}\ {\rm maximal}\ {\rm with}\ {\rm respect}\ {\rm to}\ i=1,2,\dots,2^{N}-1\right\}.$$
Note that for every $x\in Q$, $$D_k^{\nu}(b)(x)=\sum_{i=1}^{2^N-1}\langle b,h_Q^i\rangle h_Q^i(x),\ {\rm with}\ \|h_Q^i\|_{\infty}\lesssim w(Q)^{-1/2}.$$ Hence, we have
\begin{align}
  \bigg(\fint_Q |D_k^\nu(b)(x)|^pdw(x)\bigg)^{1/p}\leq C_{p,N} w(Q)^{-1/2}\bigg|\int_Qb(x)h_Q(x)dw(x)\bigg|
\end{align}
for some constant $C_{p,N}>0$.
This, together with the doubling inequality \eqref{doublinginequality}, implies
\begin{align}\label{hhhhhh}
  \int_{\mathbb{R}^N}\frac{|D_k^\nu(b)(x)|^p}{w(B(x,2^{-k}))}dw(x)
  &\lesssim \sum_{Q\in\mathbb{D}_k^\nu}\fint_Q|D_k^\nu(b)(x)|^pdw(x)\nonumber\\
  &\lesssim \sum_{Q\in\mathbb{D}_k^\nu}w(Q)^{-p/2}|\langle b,h_Q\rangle_{L^2(\mathbb{R}^N,dw)}|^p.
\end{align}
By Lemma \ref{NWOlemma}, there exist NWO sequences $\{u_{Q}^{(s)}\}$ and $\{v_{Q}^{(s)}\}$, $s=1,2,3,4$, such that
\begin{align}\label{yieldssss}
  |\langle b,h_Q\rangle_{L^2(\mathbb{R}^N,dw)}|&\lesssim w(Q)^{1/2}\sum_{s=1}^4|\langle [b,R_{\D,j}]u_{Q}^{(s)},v_{Q}^{(s)}\rangle_{L^2(\mathbb{R}^N,dw)}|.
\end{align}
Substituting \eqref{yieldssss} into \eqref{hhhhhh} and then applying Lemma \ref{NWOLEMMAA}, we deduce that
\begin{align*}
  \sum_{k\in\mathbb{Z}}\int_{\mathbb{R}^N}\frac{|D_k^\nu(b)(x)|^p}{w(B(x,2^{-k}))}dw(x)
  &\lesssim \sum_{s=1}^4\sum_{k\in\mathbb{Z}}\sum_{Q\in\mathbb{D}_k^\nu}|\langle [b,R_{\D,j}]u_{Q}^{(s)},v_{Q}^{(s)}\rangle_{L^2(\mathbb{R}^N,dw)}|^p\nonumber\\
  &\lesssim \|[b,R_{\D,j}]\|_{S^p}^p.
\end{align*}
This shows the inequality \eqref{martingaleassertion} and then finishes the proof of Lemma \ref{standard}.
\end{proof}



\section{Constant characterization}
\setcounter{equation}{0}

\begin{theorem} If $f\in L_1^{{\rm loc}}(\mathbb{R}^N,w(x)dx)$ is such that $$[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_p(L_2(\mathbb{R}^N,w(x)dx))$$
for some $p\leq d,$ then $f$ is constant.
\end{theorem}
\begin{proof} By Lemma \ref{hytonen verification lemma}, measures $dx$ and $w(x)dx$ and operator $T_{\Gamma,j}=M_{\chi_{\Gamma}}R_{{\rm Dunkl},j}M_{\chi_{\Gamma}}$ satisfy {\color{red} on $\Gamma$} the assumptions in Theorem 1.4 in \cite{Hytonen-new}. By assumption, $[T_{\Gamma,j},M_f]\in\mathcal{L}_p(L_2(\Gamma,w(x)dx)).$ By Theorem 1.4 in \cite{Hytonen-new}, $f$ is constant on $\Gamma.$ 

Hence, $f$ is constant on every chamber. Let $f=c_1$ on $\Gamma_1$ and $f=c_2$ on $\Gamma_2.$ On the other hand, $[R_{{\rm Dunkl},j},M_f]\in\mathcal{L}_{2N}(L_2(\mathbb{R}^N,w(x)dx)).$ By Theorem \ref{main theorem} \eqref{mtb}, $f\in\dot{W}^{\frac12,2N}(\mathbb{R}^N).$  We have
$$\int_{\Gamma_1\times\Gamma_2}\frac{|c_1-c_2|^{2N}dxdy}{|x-y|^{2N}}=\int_{\Gamma_1\times\Gamma_2}\frac{|f(x)-f(y)|^{2N}dxdy}{|x-y|^{2N}}\leq$$
$$\leq\int_{\mathbb{R}^N\times\mathbb{R}^N}\frac{|f(x)-f(y)|^{2N}dxdy}{|x-y|^{2N}}=\|f\|_{\dot{W}^{\frac12,2N}(\mathbb{R}^N)}<\infty.$$
It follows that $c_1=c_2.$ Since chambers $\Gamma_1$ and $\Gamma_2$ are arbitrary, the assertion follows.
\end{proof}


 \noindent
 {\bf Acknowledgements:}

%The authors would like to thank Rambau J\"{o}rg, Christian Kipp, Jes\'{u}s De Loera, Santos Leal  Francisco, Arnau Padrol for helpful discussion on triangulation of polyhedral cone.
Z. Fan is supported by the Guangdong Basic and Applied Basic Research Foundation (No. 2023A1515110879). J. Li is supported by the Australian Research Council (ARC) through the research grant DP220100285.

\bibliographystyle{plain}
\bibliography{references}

\end{document}
